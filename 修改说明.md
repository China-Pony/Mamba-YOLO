
# Cursor 中使用Opus 4.7



**我要将FDConv卷积模块应用到Mamba-YOLO模型中，我有三种方案：

1. 将Mamba-YOLO中的RG Block模块中的DW-Conv模块替换为FDConv卷积

2. 将Mamba-YOLO中的Simple Stem模块中的Conv替换为FDConv卷积

3. 将Mamba-YOLO中的ODSSBlock中的1×1 Conv替换为FDConv卷积

请你给每种方案提供一个方案分析以及详细的代码修改指南**





我已经通读了 Mamba-YOLO/ultralytics/nn/modules/mamba_yolo.py（RGBlock、SimpleStem、XSSBlock、VSSBlock）和 FDConv/FDConv_detection/mmdet_custom/FDConv.py，下面给出 三种方案的分析 以及 详细的代码修改指南。



一、公共准备工作（三种方案都需要做一次）

FDConv 原实现依赖 mmcv.cnn.CONV_LAYERS，Mamba‑YOLO 走的是 ultralytics 体系，没有 mmcv。所以第一步是把 FDConv 移植进来并移除 mmcv 依赖。

1. 复制文件

把 FDConv/FDConv_detection/mmdet_custom/FDConv.py 复制一份放到：

Mamba-YOLO/ultralytics/nn/modules/FDConv.py

2. 删除 mmcv 相关代码

打开新复制的 Mamba-YOLO/ultralytics/nn/modules/FDConv.py，删除/注释两处：

from mmcv.cnn import CONV_LAYERS      # ← 删除这一行

...

@CONV_LAYERS.register_module()        # ← 删除 / 注释掉这一行装饰器

class FDConv(nn.Conv2d):

    ...

同时把文件里没必要的 matplotlib、matrix_rank 等测试用 import 也可以删掉，保持依赖干净：

import numpy as np

# import matplotlib.pyplot as plt

# from numpy.linalg import matrix_rank

3. 在 __init__.py 中暴露 FDConv

修改 Mamba-YOLO/ultralytics/nn/modules/__init__.py（第 79 行那块）：

__init__.pyLines 79-80

from .mamba_yolo import SimpleStem, VisionClueMerge, VSSBlock, XSSBlock

from .FDConv import FDConv



并把 "FDConv" 加进 __all__。

4. 在 mamba_yolo.py 顶部导入 FDConv

# Mamba-YOLO/ultralytics/nn/modules/mamba_yolo.py

from .common_utils_mbyolo import *

from .FDConv import FDConv   # 新增

做完这 4 步后，三种方案就只需要改 mamba_yolo.py 里的局部代码。



二、方案 1：把 RGBlock 里的 DW‑Conv 替换为 FDConv

方案分析

RGBlock 对应论文的 Residual Gated Block（MLP 分支），其中关键的局部建模就是那个 3×3 深度卷积：

mamba_yolo.pyLines 240-252

self.dwconv = nn.Conv2d(hidden_features, hidden_features, kernel_size=3, stride=1, padding=1, bias=True,

                        groups=hidden_features)

...

def forward(self, x):

    x, v = self.fc1(x).chunk(2, dim=1)

    x = self.act(self.dwconv(x) + x) * v

    ...



优点：

DW 卷积本身 FLOPs 很小，替换成 FDConv 带来的额外 FLOPs 在 RGBlock 这一层占比相对可控。

因为 in_channels == out_channels == groups，FDConv 里的 func_filter 会自动走 skip，参数增量比 point‑wise 替换更小。

3×3 kernel 正好命中 FDConv 的 use_fbm_if_k_in=[3]，会额外启用 FBM（频率带调制），理论上对 Mamba 忽略的高频细节最有帮助——这是三种方案里与论文 motivation 契合度最高 的。

注意点：

RGBlock 在 XSSBlock/VSSBlock 里每个 stage 都会出现多次，调用频次高，训练显存会明显上升。

小模型（Mamba‑YOLO‑T，width=0.25）的 hidden_features 可能小于 16，FDConv 内部会自动回退到普通 nn.Conv2d，所以不用担心小通道数下崩溃，只是对小模型收益有限。

由于 FDConv 自带 BN/权重初始化逻辑，会把原本 bias=True 的 DW 行为微改，但等价性不影响训练。

代码修改指南

在 Mamba-YOLO/ultralytics/nn/modules/mamba_yolo.py 里定位到 RGBlock 类：

mamba_yolo.pyLines 232-252

class RGBlock(nn.Module):

    def __init__(self, in_features, hidden_features=None, out_features=None, act_layer=nn.GELU, drop=0.,

                 channels_first=False):

        super().__init__()

        out_features = out_features or in_features

        hidden_features = hidden_features or in_features

        hidden_features = int(2 * hidden_features / 3)

        self.fc1 = nn.Conv2d(in_features, hidden_features * 2, kernel_size=1)

        self.dwconv = nn.Conv2d(hidden_features, hidden_features, kernel_size=3, stride=1, padding=1, bias=True,

                                groups=hidden_features)



把 self.dwconv 这一行整体替换为：

self.dwconv = FDConv(

    in_channels=hidden_features,

    out_channels=hidden_features,

    kernel_size=3,

    stride=1,

    padding=1,

    groups=hidden_features,     # 仍然保持深度可分离特性

    bias=True,

    kernel_num=4,               # 论文里小模型常用 4，通道大时可改 8

    reduction=0.0625,

    use_fdconv_if_c_gt=16,      # hidden<16 时自动回退 nn.Conv2d，对 T 尺度友好

    use_fbm_if_k_in=[3],        # 3x3 启用 FBM，针对高频信息调制

)



其他部分（fc1、fc2、forward）完全不用改，因为 FDConv 的接口与 nn.Conv2d 兼容。



三、方案 2：把 SimpleStem 中的 Conv 替换为 FDConv

方案分析

SimpleStem 是网络的第一层 Stem，两层 stride=2 的 3×3 卷积：

mamba_yolo.pyLines 450-464

class SimpleStem(nn.Module):

    def __init__(self, inp, embed_dim, ks=3):

        super().__init__()

        self.hidden_dims = embed_dim // 2

        self.conv = nn.Sequential(

            nn.Conv2d(inp, self.hidden_dims, kernel_size=ks, stride=2, padding=autopad(ks, d=1), bias=False),

            nn.BatchNorm2d(self.hidden_dims),

            nn.GELU(),

            nn.Conv2d(self.hidden_dims, embed_dim, kernel_size=ks, stride=2, padding=autopad(ks, d=1), bias=False),

            nn.BatchNorm2d(embed_dim),

            nn.SiLU(),

        )



分析：

第一层 inp=3（RGB）→ min(in, out)=3 ≤ use_fdconv_if_c_gt(16)，FDConv 会在 forward 中 自动走 super().forward(x) 回退到普通 Conv2d，也就是说第一层替换了也等价于没换。推荐 只替换第二层（或者把阈值设为更低）。

第二层 hidden_dims → embed_dim 一般 ≥ 64，可正常启用 FDConv。

FDConv 的 forward 在 stride=2 下是支持的（它没有对 stride 做硬性拦截），但默认 use_fbm_if_k_in=[3] 会导致 FBM 也启用；如果担心频谱带调制在高分辨率大特征图上显存/耗时高，可以把 use_fbm_if_k_in=[] 关掉 FBM。

Stem 在每个 forward 只执行一次，增加的显存/计算非常有限，性价比很高，但对整体精度收益也相对最小（因为 Stem 只影响浅层细节）。

注意点：

FDConv 内置偏置、无 BN；Mamba‑YOLO 的原 Stem 是 Conv → BN → GELU，保留外层的 BN/激活即可。

空间维度还是 640×640 级别，FBM 里会做 rfft2，训练初期显存峰值会比原 Stem 明显增加，建议在 24G 以上卡或小 batch 测试。

代码修改指南

把 SimpleStem 的 __init__ 直接改为：

class SimpleStem(nn.Module):

    def __init__(self, inp, embed_dim, ks=3):

        super().__init__()

        self.hidden_dims = embed_dim // 2

        self.conv = nn.Sequential(

            FDConv(

                in_channels=inp,

                out_channels=self.hidden_dims,

                kernel_size=ks,

                stride=2,

                padding=autopad(ks, d=1),

                bias=False,

                kernel_num=4,

                use_fdconv_if_c_gt=16,   # inp=3 时会自动退化为普通 Conv2d

                use_fbm_if_k_in=[3],

                use_fbm_for_stride=True, # stride>1 也启用 FBM

            ),

            nn.BatchNorm2d(self.hidden_dims),

            nn.GELU(),

            FDConv(

                in_channels=self.hidden_dims,

                out_channels=embed_dim,

                kernel_size=ks,

                stride=2,

                padding=autopad(ks, d=1),

                bias=False,

                kernel_num=4,

                use_fdconv_if_c_gt=16,

                use_fbm_if_k_in=[3],

                use_fbm_for_stride=True,

            ),

            nn.BatchNorm2d(embed_dim),

            nn.SiLU(),

        )



    def forward(self, x):

        return self.conv(x)



如果只想替换第二层（更稳、更省显存）：

self.conv = nn.Sequential(

    nn.Conv2d(inp, self.hidden_dims, kernel_size=ks, stride=2, padding=autopad(ks, d=1), bias=False),

    nn.BatchNorm2d(self.hidden_dims),

    nn.GELU(),

    FDConv(

        in_channels=self.hidden_dims,

        out_channels=embed_dim,

        kernel_size=ks,

        stride=2,

        padding=autopad(ks, d=1),

        bias=False,

        kernel_num=4,

        use_fbm_for_stride=True,

    ),

    nn.BatchNorm2d(embed_dim),

    nn.SiLU(),

)





四、方案 3：把 ODSSBlock（XSSBlock/VSSBlock）里的 1×1 Conv 替换为 FDConv

方案分析

论文里的 ODSSBlock 对应代码中的 XSSBlock/VSSBlock。真正的 1×1 Conv 有两个位置：

XSSBlock.in_proj：Conv2d(in_channels, hidden_dim, 1)

VSSBlock.proj_conv：Conv2d(in_channels, hidden_dim, 1)

mamba_yolo.pyLines 321-325

self.in_proj = nn.Sequential(

    nn.Conv2d(in_channels, hidden_dim, kernel_size=1, stride=1, padding=0, bias=False),

    nn.BatchNorm2d(hidden_dim),

    nn.SiLU()

) if in_channels != hidden_dim else nn.Identity()



mamba_yolo.pyLines 394-399

self.proj_conv = nn.Sequential(

    nn.Conv2d(in_channels, hidden_dim, kernel_size=1, stride=1, padding=0, bias=True),

    nn.BatchNorm2d(hidden_dim),

    nn.SiLU()

)



分析：

kernel_size=1 满足 FDConv 默认 use_fdconv_if_k_in=[1, 3]。

1×1 情况下 FDConv 内部：

func_spatial=skip（空间注意力无效）

FBM 默认不启用（use_fbm_if_k_in=[3]）

主要起作用的是 KSM_Global 的 channel/filter/kernel 注意力 + FDW 频域权重分解，这其实就是 ODConv/DyConv 思路在频域上的扩展。

in_proj 是 ODSSBlock 的入口通道投影，每个 block 只执行一次，且作用于整个 block 的输入表达，理论收益很大；开销在于投影通道规模（例如 1024）可能比较大，FDW 的参数会增加。

XSSBlock 的 in_proj 当 in_channels == hidden_dim 时是 Identity，这种情况不会有卷积替换（例如 head 里会出现）。

1×1 Conv 没有 FBM，因此这一方案不会用到频带调制，理论上最贴合 ODConv 的动态卷积思路，是三种方案中偏"通道自适应"的路线。

注意点：

小模型下 in_channels 可能较小（64/128），仍然 > 16，会启用 FDConv。但 kernel_num=4 的参数增量在 1×1 情况下几乎等价于 + 4 × C_in × C_out / param_reduction 的 FDW 系数，对大通道数模型（B/L 尺度）显存压力更大。

FDConv 默认构造时会 print('*** kernel_num:', ...)，大量 block 训练时日志会刷屏，建议把 FDConv.py 里那行 print 注释掉。

代码修改指南

（1）替换 XSSBlock.in_proj 的 1×1 Conv

class XSSBlock(nn.Module):

    def __init__(self, in_channels=0, hidden_dim=0, n=1, mlp_ratio=4.0, ...):

        super().__init__()



        self.in_proj = nn.Sequential(

            FDConv(

                in_channels=in_channels,

                out_channels=hidden_dim,

                kernel_size=1,

                stride=1,

                padding=0,

                bias=False,

                kernel_num=4,

                use_fdconv_if_c_gt=16,

                use_fdconv_if_k_in=[1, 3],

            ),

            nn.BatchNorm2d(hidden_dim),

            nn.SiLU()

        ) if in_channels != hidden_dim else nn.Identity()

        ...



（2）替换 VSSBlock.proj_conv 的 1×1 Conv

class VSSBlock(nn.Module):

    def __init__(self, in_channels=0, hidden_dim=0, ...):

        super().__init__()

        ...

        self.proj_conv = nn.Sequential(

            FDConv(

                in_channels=in_channels,

                out_channels=hidden_dim,

                kernel_size=1,

                stride=1,

                padding=0,

                bias=True,

                kernel_num=4,

                use_fdconv_if_c_gt=16,

                use_fdconv_if_k_in=[1, 3],

            ),

            nn.BatchNorm2d(hidden_dim),

            nn.SiLU()

        )

        ...

其余部分（SS2D、LSBlock、mlp 分支、forward）均不需要改动。

想要更激进地替换——连 SS2D.in_proj/SS2D.out_proj 和 LSBlock 里的 1×1 Conv 一起换——思路完全一致，只是替换点更多。不建议一次全换，先锁定 in_proj/proj_conv 做消融。



五、三种方案对比总结

对比维度

方案 1：RGBlock.dwconv

方案 2：SimpleStem.Conv

方案 3：ODSSBlock 1×1

替换数量

每个 block 1 个，整网几十个

整网 1~2 个

每个 block 1 个，整网十几个

核函数

3×3 DW

3×3 stride=2

1×1

启用模块

KSM + FDW + FBM

KSM + FDW + FBM（stride 时开）

KSM + FDW（无 FBM/Spatial）

与论文 motivation 贴合度

★★★（高频/细节补偿）

★★（仅 Stem 浅层）

★★★（频域动态卷积思路）

参数/显存开销

中（DW 开销小）

低（只在 Stem）

高（大通道 1×1）

预期精度收益

较高（细节 + 多 stage）

较低（只影响浅层）

中偏高（每 stage 入口都动态投影）

风险

训练显存上升，多处扩展

大特征图下 FBM 显存峰值

大通道下 FDW 系数大，需关注过拟合

推荐做法

作为主实验方案

作为轻量对照实验

做消融时与方案 1 叠加



建议的推进顺序：先用方案 2 打通工程链路（改动最小、最不容易出 bug）→ 用方案 1 做主实验（收益高）→ 再用方案 3 做消融叠加。



六、验证修改是否成功

完成任一方案后，用下面的命令快速跑一次前向，确认没有 shape/dtype 报错：

# tests/test_fdconv_mbyolo.py

import torch

from ultralytics.nn.modules.mamba_yolo import RGBlock, SimpleStem, XSSBlock, VSSBlock



x = torch.randn(2, 128, 32, 32).cuda()



rg = RGBlock(in_features=128, hidden_features=256).cuda()

print('RGBlock ok:', rg(x).shape)



stem = SimpleStem(3, 128).cuda()

print('Stem ok:', stem(torch.randn(2, 3, 640, 640).cuda()).shape)



xss = XSSBlock(in_channels=128, hidden_dim=256, n=1).cuda()

print('XSSBlock ok:', xss(x).shape)



vss = VSSBlock(in_channels=128, hidden_dim=256).cuda()

print('VSSBlock ok:', vss(x).shape)



以及用原有训练脚本做一次 --epochs 1 --batch 2 的 smoke test，能跑完一个 iter 基本就说明移植无误。

如果你确定先上手哪一个方案，我可以直接帮你把对应的 mamba_yolo.py 和 __init__.py 改写为最终可编译的完整版本。

















在你的方案1中，在RGBlock中在reshape时未考虑分组卷积导致的维度变化，错误地假设了in_channels//groups == in_channels(即groups=1)，给出修改方案。

















你指出的 bug 很关键。我重新对照了 FDConv.py，确认了在多处 reshape 时都默认了 cin_per_group == self.in_channels，只有在 groups == 1 时才成立。深度卷积（groups == in_channels == out_channels）会直接 reshape 报错。下面先把 有问题的具体位置 列出来，再给出 两套可选的修复方案。



一、Bug 定位

PyTorch 中 nn.Conv2d 的权重形状是：

weight.shape == (out_channels, in_channels // groups, kH, kW)

而 FDConv 里多处都把 cin 当成了 self.in_channels。核心出错点有 4 处：

① convert2dftweight（line 604 起）

FDConv.pyLines 604-608

def convert2dftweight(self, convert_param):

    d1, d2, k1, k2 = self.out_channels, self.in_channels, self.kernel_size[0], self.kernel_size[1]

    freq_indices, _ = get_fft2freq(d1 * k1, d2 * k2, use_rfft=True)

    weight = self.weight.permute(0, 2, 1, 3).reshape(d1 * k1, d2 * k2)



self.weight 元素数是 d1 * (d2 // groups) * k1 * k2，reshape 为 (d1*k1, d2*k2) 在 DW 下会直接抛 RuntimeError。

② get_FDW（line 633 起）

FDConv.pyLines 633-638

def get_FDW(self, ):

    d1, d2, k1, k2 = self.out_channels, self.in_channels, self.kernel_size[0], self.kernel_size[1]

    weight = self.weight.reshape(d1, d2, k1, k2).permute(0, 2, 1, 3).reshape(d1 * k1, d2 * k2)



同样的问题。

③ forward 中的 DFT_map 和 adaptive_weights（line 661、683）

FDConv.pyLines 661-684

DFT_map = torch.zeros((b, self.out_channels * self.kernel_size[0], self.in_channels * self.kernel_size[1] // 2 + 1, 2), device=x.device)

...

adaptive_weights = torch.fft.irfft2(torch.view_as_complex(DFT_map), dim=(1, 2)).reshape(batch_size, 1, self.out_channels, self.kernel_size[0], self.in_channels, self.kernel_size[1])

adaptive_weights = adaptive_weights.permute(0, 1, 2, 4, 3, 5)



这里把 cin 按 self.in_channels 展开，但下方真正用于 F.conv2d 的权重却要求 (..., in_channels // groups, k, k)：

FDConv.pyLines 696-700

aggregate_weight = aggregate_weight.view(

    [-1, self.in_channels // self.groups, self.kernel_size[0], self.kernel_size[1]])



两侧元素数对不上，DW 情况下 view 会把 b*C*C*k*k 当作 b*C*C 个 1*k*k 核来分发，完全错位。

④ hr_att_logit（line 648）和 KSM_Global.channel_fc 的维度

FDConv.pyLines 648-650

hr_att_logit = hr_att_logit.reshape(x.size(0), 1, self.in_channels, self.out_channels, self.kernel_size[0], self.kernel_size[1])

hr_att_logit = hr_att_logit.permute(0, 1, 3, 2, 4, 5)



此处 cin 也按 self.in_channels 处理，同时 KSM_Global.channel_fc 的输出通道也是 in_planes，它们与 adaptive_weights 的 cin 轴都应按 in_channels // groups 对齐。



二、修复方案

方案 A（推荐、最小侵入）：给 FDConv 打 group‑aware 补丁

核心思想：FDConv 对外 API 不变，内部所有"kernel 的 cin 维"统一改为 self.in_channels_per_group。下面给出可直接替换的完整 diff。

步骤 1：__init__ 中新增 in_channels_per_group 并把它传给 KSM

在 FDConv.__init__ 里 super().__init__(*args, **kwargs) 之后立刻加一行：

super().__init__(*args, **kwargs)



# 新增：卷积权重真正的输入通道维 = in_channels // groups

self.in_channels_per_group = self.in_channels // self.groups

步骤 2：修正 convert2dftweight

把 l.604-619 改成（只改 d2 的定义，其它保持不变）：

def convert2dftweight(self, convert_param):

    # d2 使用分组后的真实 cin（= in_channels // groups）

    d1 = self.out_channels

    d2 = self.in_channels_per_group

    k1, k2 = self.kernel_size[0], self.kernel_size[1]



    freq_indices, _ = get_fft2freq(d1 * k1, d2 * k2, use_rfft=True)

    weight = self.weight.permute(0, 2, 1, 3).reshape(d1 * k1, d2 * k2)

    weight_rfft = torch.fft.rfft2(weight, dim=(0, 1))

    if self.param_reduction < 1:

        num_to_keep = int(freq_indices.size(1) * self.param_reduction)

        freq_indices = freq_indices[:, :num_to_keep]

        weight_rfft = torch.stack([weight_rfft.real, weight_rfft.imag], dim=-1)

        weight_rfft = weight_rfft[freq_indices[0, :], freq_indices[1, :]]

        weight_rfft = weight_rfft.reshape(-1, 2)[None, ].repeat(self.param_ratio, 1, 1) / max(1, (min(self.out_channels, self.in_channels_per_group) // 2))

    else:

        weight_rfft = torch.stack([weight_rfft.real, weight_rfft.imag], dim=-1)[None, ].repeat(self.param_ratio, 1, 1, 1) / max(1, (min(self.out_channels, self.in_channels_per_group) // 2))



注意 alpha 里的分母也要跟着改：

# 旧：

# self.alpha = min(self.out_channels, self.in_channels) // 2 * self.kernel_num * self.param_ratio / param_reduction

# 新：

self.alpha = max(1, min(self.out_channels, self.in_channels_per_group) // 2) * self.kernel_num * self.param_ratio / param_reduction



（对 DW conv 来说 in_channels_per_group == 1，// 2 == 0，所以外套一层 max(1, ...) 防止除 0）

步骤 3：修正 get_FDW

def get_FDW(self):

    d1 = self.out_channels

    d2 = self.in_channels_per_group

    k1, k2 = self.kernel_size[0], self.kernel_size[1]

    weight = self.weight.reshape(d1, d2, k1, k2).permute(0, 2, 1, 3).reshape(d1 * k1, d2 * k2)

    weight_rfft = torch.fft.rfft2(weight, dim=(0, 1)).contiguous()

    weight_rfft = torch.stack([weight_rfft.real, weight_rfft.imag], dim=-1)[None, ].repeat(self.param_ratio, 1, 1, 1) \

                  / max(1, (min(self.out_channels, self.in_channels_per_group) // 2))

    return weight_rfft



步骤 4：修正 forward 里的 DFT_map / adaptive_weights / hr_att_logit

把原 forward 改为：

def forward(self, x):

    if min(self.in_channels, self.out_channels) <= self.use_fdconv_if_c_gt \

            or self.kernel_size[0] not in self.use_fdconv_if_k_in:

        return super().forward(x)



    # 真正的 kernel cin 维（分组后）

    cin_g = self.in_channels_per_group

    k1, k2 = self.kernel_size[0], self.kernel_size[1]



    global_x = F.adaptive_avg_pool2d(x, 1)

    channel_attention, filter_attention, spatial_attention, kernel_attention = self.KSM_Global(global_x)



    if self.use_ksm_local:

        hr_att_logit = self.KSM_Local(global_x)

        # 用 cin_g 代替 self.in_channels

        hr_att_logit = hr_att_logit.reshape(x.size(0), 1, cin_g, self.out_channels, k1, k2)

        hr_att_logit = hr_att_logit.permute(0, 1, 3, 2, 4, 5)

        if self.ksm_local_act == 'sigmoid':

            hr_att = hr_att_logit.sigmoid() * self.att_multi

        elif self.ksm_local_act == 'tanh':

            hr_att = 1 + hr_att_logit.tanh()

        else:

            raise NotImplementedError

    else:

        hr_att = 1



    b = x.size(0)

    batch_size, in_planes, height, width = x.size()



    # DFT_map 的第二、三维也按 cin_g 构造

    DFT_map = torch.zeros((b, self.out_channels * k1, cin_g * k2 // 2 + 1, 2), device=x.device)

    kernel_attention = kernel_attention.reshape(b, self.param_ratio, self.kernel_num, -1)



    dft_weight = self.dft_weight if hasattr(self, 'dft_weight') else self.get_FDW()



    for i in range(self.param_ratio):

        indices = self.indices[i]

        if self.param_reduction < 1:

            w = dft_weight[i].reshape(self.kernel_num, -1, 2)[None]

        else:

            w = dft_weight[i][indices[0, :, :], indices[1, :, :]][None] * self.alpha

        DFT_map[:, indices[0, :, :], indices[1, :, :]] += torch.stack(

            [w[..., 0] * kernel_attention[:, i], w[..., 1] * kernel_attention[:, i]], dim=-1)



    # reshape 到 (b, 1, cout, k1, cin_g, k2),再 permute 为 (b, 1, cout, cin_g, k1, k2)

    adaptive_weights = torch.fft.irfft2(

        torch.view_as_complex(DFT_map), dim=(1, 2)

    ).reshape(batch_size, 1, self.out_channels, k1, cin_g, k2)

    adaptive_weights = adaptive_weights.permute(0, 1, 2, 4, 3, 5)



    if hasattr(self, 'FBM'):

        x = self.FBM(x)



    # channel_attention / filter_attention / spatial_attention 的形状

    # 经过步骤 5 的补丁后,已经分别是 (b,1,1,cin_g,1,1)、(b,1,cout,1,1,1)、(b,1,1,1,k1,k2)

    aggregate_weight = spatial_attention * channel_attention * filter_attention * adaptive_weights * hr_att

    aggregate_weight = torch.sum(aggregate_weight, dim=1)

    aggregate_weight = aggregate_weight.view([-1, cin_g, k1, k2])



    x = x.reshape(1, -1, height, width)

    output = F.conv2d(

        x, weight=aggregate_weight, bias=None,

        stride=self.stride, padding=self.padding, dilation=self.dilation,

        groups=self.groups * batch_size,

    )

    output = output.view(batch_size, self.out_channels, output.size(-2), output.size(-1))

    if self.bias is not None:

        output = output + self.bias.view(1, -1, 1, 1)

    return output



这里顺带把原来只在 else 分支里用 filter_attention.view(b, -1, 1, 1) 的分支逻辑合并了，把 filter/channel 注意力统一作用在 aggregate_weight 上，保证 DW 情况下不出错。

步骤 5：修正 KSM_Global 的 channel_fc 输出维度（关键）

在 FDConv 构造 KSM_Global 的那一行，把 in_channels 换成 in_channels_per_group，并显式告知 groups：

# 原：

# self.KSM_Global = KernelSpatialModulation_Global(

#     self.in_channels, self.out_channels, self.kernel_size[0], groups=self.groups, ...

# )



# 改：

self.KSM_Global = KernelSpatialModulation_Global(

    self.in_channels_per_group,       # ← 改成分组后的 cin

    self.out_channels,

    self.kernel_size[0],

    groups=1,                          # 这里是 KSM 自身的判断用,对 DW 要给 1 才能正常出 channel_attention

    temp=temp, kernel_temp=kernel_temp,

    reduction=reduction,

    kernel_num=self.kernel_num * self.param_ratio,

    kernel_att_init=None,

    att_multi=att_multi,

    ksm_only_kernel_att=ksm_only_kernel_att,

    act_type=self.ksm_global_act,

    att_grid=att_grid,

    stride=self.stride,

    spatial_freq_decompose=spatial_freq_decompose,

)



与此同时，fc（输入 avg‑pooled 特征→注意力空间）仍然必须用真实的 in_channels，所以 KSM_Global 的 self.fc 要单独改回：

在 KSM_Global.__init__ 内把原来的：

self.fc = nn.Conv2d(in_planes, attention_channel, 1, bias=False)

改为：

# in_planes 现在是 in_channels // groups,但输入特征的通道依然是原 in_channels

# 使用新增参数 in_planes_for_input 来区分

self.fc = nn.Conv2d(kwargs.get('in_planes_for_input', in_planes), attention_channel, 1, bias=False)



并在 FDConv 里构造 KSM_Global 时传入：

self.KSM_Global = KernelSpatialModulation_Global(

    self.in_channels_per_group, self.out_channels, self.kernel_size[0],

    groups=1, in_planes_for_input=self.in_channels,

    ...

)



步骤 6：修正 KSM_Local

原来：

self.KSM_Local = KernelSpatialModulation_Local(

    channel=self.in_channels, kernel_num=1,

    out_n=int(self.out_channels * self.kernel_size[0] * self.kernel_size[1]),

)



对 DW conv，KSM_Local 输出的 cin 轴长度必须等于 in_channels_per_group=1。把它改为：

self.KSM_Local = KernelSpatialModulation_Local(

    channel=self.in_channels_per_group, kernel_num=1,

    out_n=int(self.out_channels * self.kernel_size[0] * self.kernel_size[1]),

)



同时在 forward 里调用它之前把 global_x 先按组做平均（不同组合并）：

if self.use_ksm_local:

    if self.groups > 1:

        # (b, C, 1, 1) -> (b, groups, C//groups, 1, 1) -> (b, C//groups, 1, 1)

        gx = global_x.view(b, self.groups, cin_g, 1, 1).mean(dim=1)

    else:

        gx = global_x

    hr_att_logit = self.KSM_Local(gx)

    ...



步骤 7：对 param_reduction < 1 的防御性处理

DW 的 d1*k1 × (d2*k2//2 + 1) 频谱点数会非常少（例如 C=64, k=3 时只有 192×2=384 个复数点）。self.alpha 分母 min(cout, cin_g)//2 = 0，前面已经用 max(1, ...) 处理过；num_to_keep 也要保证 ≥ 1：

num_to_keep = max(1, int(freq_indices.size(1) * self.param_reduction))





方案 B（最省事、但损失 DW 语义）：RGBlock 里别再用 groups 了

如果不想改 FDConv 源码，可以把 RGBlock 里那个 DW‑Conv 直接替换为普通（groups=1）的 FDConv，这样所有 reshape 都走默认路径：

# mamba_yolo.py - class RGBlock

self.dwconv = FDConv(

    in_channels=hidden_features,

    out_channels=hidden_features,

    kernel_size=3,

    stride=1,

    padding=1,

    groups=1,                    # ← 关键:不再 depthwise

    bias=True,

    kernel_num=4,

    use_fdconv_if_c_gt=16,

    use_fbm_if_k_in=[3],

)

代价：

参数量从 C·k² 涨到 C²·k²，RGBlock 的 MLP 分支会比原设计重很多。

但正因为变成 full conv，FDConv 能发挥 KSM + FDW 的完整能力，精度可能进一步提升。

适合 B/L 等中大模型；T 尺度不推荐，因为会显著拖慢。

三、建议的选择策略

场景

推荐修复

想最快跑通 RGBlock 方案、不关心 DW 高效性

方案 B（groups=1）

需要严格保留 DW 结构（和论文对齐）

方案 A（打补丁）

方案 2、方案 3（Stem / ODSSBlock 1×1）

无需改动，本来 groups=1 就没这个 bug

只有 方案 1（RGBlock DW） 会触发这个 bug；方案 2 和方案 3 因为默认都是 groups=1，不受影响。



四、验证方式

做完 A 或 B 的修改后，跑一个单元测试验证 group‑aware 路径：

import torch

from ultralytics.nn.modules.FDConv import FDConv



# 1) 普通 conv,原本就能跑

m1 = FDConv(64, 64, kernel_size=3, padding=1, groups=1, bias=True).cuda()

y1 = m1(torch.randn(2, 64, 32, 32).cuda())

assert y1.shape == (2, 64, 32, 32)



# 2) DW conv,修复前会炸,修复后应正常

m2 = FDConv(64, 64, kernel_size=3, padding=1, groups=64, bias=True).cuda()

y2 = m2(torch.randn(2, 64, 32, 32).cuda())

assert y2.shape == (2, 64, 32, 32)



# 3) 分组 conv(groups=4)

m3 = FDConv(64, 64, kernel_size=3, padding=1, groups=4, bias=True).cuda()

y3 = m3(torch.randn(2, 64, 32, 32).cuda())

assert y3.shape == (2, 64, 32, 32)



print('all group-aware forward tests passed.')



跑通这三种 groups 之后，再把 RGBlock 里换上的 FDConv 组合一起测一遍 test_fdconv_mbyolo.py，就能确认方案 1 彻底修好。

如果你希望我直接把完整打好补丁的 FDConv.py 写出来（而不是 diff 片段），告诉我一声，我把文件整体按方案 A 改写好再交给你。







**把文件整体按方案 A 改写好再交给我**











当前默认 Python 没有安装 torch，所以在这里跑不起自测。你在自己的 Mamba‑YOLO conda 环境下直接执行同一条命令即可：

conda activate <你的 Mamba-YOLO 环境>

python Mamba-YOLO/ultralytics/nn/modules/FDConv.py

预期输出：

*** FDConv kernel_num: 4 | in_channels_per_group: 64 | groups: 1

*** FDConv kernel_num: 4 | in_channels_per_group: 16 | groups: 4

*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 64

groups=1, out shape: torch.Size([2, 64, 16, 16])

groups=4, out shape: torch.Size([2, 64, 16, 16])

groups=64, out shape: torch.Size([2, 64, 16, 16])



本次修改要点速览

改动位置

改动内容

FDConv.__init__

新增 self.in_channels_per_group = in_channels // groups；新增 self._denom = max(1, min(out, cin_g)//2) 防止 DW 情况下除 0

FDConv.__init__ → KSM_Global 构造

传入 in_planes = in_channels_per_group（对齐 kernel cin 轴）；新增 in_planes_for_input = in_channels（对齐原始输入特征）

KSM_Global.__init__

新增形参 in_planes_for_input，self.fc 改用它作为输入通道数；channel_fc 继续用 in_planes

FDConv.__init__ → KSM_Local

通道数改为 in_channels_per_group；当 cin_g == 1（纯 DW）时自动置 use_ksm_local=False

KSM_Local.__init__

k_size 计算对 channel=1 做了保护，避免 log2(1)=0 的边界

FDConv.forward

KSM_Local 的输入先做"按 group 平均"，得到 (b, cin_g, 1, 1) 后再送入

FDConv.forward

DFT_map 第 3 维用 cin_g * k2 // 2 + 1 代替 in_channels * k2 // 2 + 1

FDConv.forward

adaptive_weights.reshape(..., cin_g, k2) 代替原来的 in_channels

FDConv.forward

聚合后统一用 reshape(b*cout, cin_g, k, k) 做 per‑sample dynamic conv；去除了原来两条互斥分支里错位的 filter_attention.view 逻辑

convert2dftweight / get_FDW

d2 改为 in_channels_per_group；归一化系数改为 self._denom；param_reduction < 1 分支额外保证 num_to_keep % kernel_num == 0

顶部

清理 mmcv / matplotlib / numpy.linalg 等无关依赖；保留 numpy 以兼容 torch.fft 生态



接下来的使用建议

方案 1（RGBlock DW → FDConv） 现在可以直接用：

self.dwconv = FDConv(

    in_channels=hidden_features,

    out_channels=hidden_features,

    kernel_size=3, stride=1, padding=1,

    groups=hidden_features,   # 保持 DW

    bias=True,

    kernel_num=4,

)

在 tests/test_fdconv_mbyolo.py 里补一条 DW 路径的断言，例如：

from ultralytics.nn.modules.FDConv import FDConv

dw = FDConv(32, 32, kernel_size=3, padding=1, groups=32, bias=True)

y = dw(torch.randn(2, 32, 16, 16))

assert y.shape == (2, 32, 16, 16)



如果以后想做参数精简实验，可以在 FDConv 构造时传 param_reduction=0.5 并确保 kernel_num ≤ 可保留频点数，本次补丁已经自动向下对齐到 kernel_num 的整数倍。

跑完 smoke test 如果有任何 shape 不符或 NaN/Inf，告诉我具体报错，我继续修。









# 报错，使用deepseek专家完成







```python
# mamba_yolo.py

from .common_utils_mbyolo import *
from .FDConv import FDConv

__all__ = ("VSSBlock", "SimpleStem", "VisionClueMerge", "XSSBlock")


"""
论文图 3  SS2D 模块，它封装了 SSM 的全部操作。内部包含了：

1. 输入投影：通过 1x1 卷积将通道数扩展（或分成两路，其中一路用于门控）。

2. 深度卷积（可选）：对展开前的特征做 3x3 深度卷积，增强局部建模（对应论文中的 SSM 之前的 conv）。

3. 选择性扫描核心：调用 cross_selective_scan 完成多方向 SSM。

4. 输出投影：通过 1x1 卷积将通道数映射回原尺寸。

5. 参数初始化：dt_init, A_log_init, D_init 等函数用于初始化 SSM 所需的特殊参数（如 A 矩阵、dt 偏置等），保证模型收敛。\
"""
class SS2D(nn.Module):
   def __init__(
           self,
           # basic dims ===========
           d_model=96,
           d_state=16,
           ssm_ratio=2.0,
           ssm_rank_ratio=2.0,
           dt_rank="auto",
           act_layer=nn.SiLU,
           # dwconv ===============
           d_conv=3,  # < 2 means no conv
           conv_bias=True,
           # ======================
           dropout=0.0,
           bias=False,
           # ======================
           forward_type="v2",
           **kwargs,
   ):
       """
       ssm_rank_ratio would be used in the future...
       """
       factory_kwargs = {"device": None, "dtype": None}
       super().__init__()
       d_expand = int(ssm_ratio * d_model)
       d_inner = int(min(ssm_rank_ratio, ssm_ratio) * d_model) if ssm_rank_ratio > 0 else d_expand
       self.dt_rank = math.ceil(d_model / 16) if dt_rank == "auto" else dt_rank
       self.d_state = math.ceil(d_model / 6) if d_state == "auto" else d_state  # 20240109
       self.d_conv = d_conv
       self.K = 4

       # tags for forward_type ==============================
       def checkpostfix(tag, value):
           ret = value[-len(tag):] == tag
           if ret:
               value = value[:-len(tag)]
           return ret, value

       self.disable_force32, forward_type = checkpostfix("no32", forward_type)
       self.disable_z, forward_type = checkpostfix("noz", forward_type)
       self.disable_z_act, forward_type = checkpostfix("nozact", forward_type)

       self.out_norm = nn.LayerNorm(d_inner)

       # forward_type debug =======================================
       FORWARD_TYPES = dict(
           v2=partial(self.forward_corev2, force_fp32=None, SelectiveScan=SelectiveScanCore),
       )
       self.forward_core = FORWARD_TYPES.get(forward_type, FORWARD_TYPES.get("v2", None))

       # in proj =======================================
       d_proj = d_expand if self.disable_z else (d_expand * 2)
       self.in_proj = nn.Conv2d(d_model, d_proj, kernel_size=1, stride=1, groups=1, bias=bias, **factory_kwargs)
       self.act: nn.Module = nn.GELU()

       # conv =======================================
       if self.d_conv > 1:
           self.conv2d = nn.Conv2d(
               in_channels=d_expand,
               out_channels=d_expand,
               groups=d_expand,
               bias=conv_bias,
               kernel_size=d_conv,
               padding=(d_conv - 1) // 2,
               **factory_kwargs,
           )

       # rank ratio =====================================
       self.ssm_low_rank = False
       if d_inner < d_expand:
           self.ssm_low_rank = True
           self.in_rank = nn.Conv2d(d_expand, d_inner, kernel_size=1, bias=False, **factory_kwargs)
           self.out_rank = nn.Linear(d_inner, d_expand, bias=False, **factory_kwargs)

       # x proj ============================
       self.x_proj = [
           nn.Linear(d_inner, (self.dt_rank + self.d_state * 2), bias=False,
                     **factory_kwargs)
           for _ in range(self.K)
       ]
       self.x_proj_weight = nn.Parameter(torch.stack([t.weight for t in self.x_proj], dim=0))  # (K, N, inner)
       del self.x_proj

       # out proj =======================================
       self.out_proj = nn.Conv2d(d_expand, d_model, kernel_size=1, stride=1, bias=bias, **factory_kwargs)
       self.dropout = nn.Dropout(dropout) if dropout > 0. else nn.Identity()

       # simple init dt_projs, A_logs, Ds
       self.Ds = nn.Parameter(torch.ones((self.K * d_inner)))
       self.A_logs = nn.Parameter(
           torch.zeros((self.K * d_inner, self.d_state)))  # A == -A_logs.exp() < 0; # 0 < exp(A * dt) < 1
       self.dt_projs_weight = nn.Parameter(torch.randn((self.K, d_inner, self.dt_rank)))
       self.dt_projs_bias = nn.Parameter(torch.randn((self.K, d_inner)))

   @staticmethod
   def dt_init(dt_rank, d_inner, dt_scale=1.0, dt_init="random", dt_min=0.001, dt_max=0.1, dt_init_floor=1e-4,
               **factory_kwargs):
       dt_proj = nn.Linear(dt_rank, d_inner, bias=True, **factory_kwargs)

       # Initialize special dt projection to preserve variance at initialization
       dt_init_std = dt_rank ** -0.5 * dt_scale
       if dt_init == "constant":
           nn.init.constant_(dt_proj.weight, dt_init_std)
       elif dt_init == "random":
           nn.init.uniform_(dt_proj.weight, -dt_init_std, dt_init_std)
       else:
           raise NotImplementedError

       # Initialize dt bias so that F.softplus(dt_bias) is between dt_min and dt_max
       dt = torch.exp(
           torch.rand(d_inner, **factory_kwargs) * (math.log(dt_max) - math.log(dt_min))
           + math.log(dt_min)
       ).clamp(min=dt_init_floor)
       # Inverse of softplus: https://github.com/pytorch/pytorch/issues/72759
       inv_dt = dt + torch.log(-torch.expm1(-dt))
       with torch.no_grad():
           dt_proj.bias.copy_(inv_dt)
       # Our initialization would set all Linear.bias to zero, need to mark this one as _no_reinit
       # dt_proj.bias._no_reinit = True

       return dt_proj

   @staticmethod
   def A_log_init(d_state, d_inner, copies=-1, device=None, merge=True):
       # S4D real initialization
       A = repeat(
           torch.arange(1, d_state + 1, dtype=torch.float32, device=device),
           "n -> d n",
           d=d_inner,
       ).contiguous()
       A_log = torch.log(A)  # Keep A_log in fp32
       if copies > 0:
           A_log = repeat(A_log, "d n -> r d n", r=copies)
           if merge:
               A_log = A_log.flatten(0, 1)
       A_log = nn.Parameter(A_log)
       A_log._no_weight_decay = True
       return A_log

   @staticmethod
   def D_init(d_inner, copies=-1, device=None, merge=True):
       # D "skip" parameter
       D = torch.ones(d_inner, device=device)
       if copies > 0:
           D = repeat(D, "n1 -> r n1", r=copies)
           if merge:
               D = D.flatten(0, 1)
       D = nn.Parameter(D)  # Keep in fp32
       D._no_weight_decay = True
       return D

   def forward_corev2(self, x: torch.Tensor, channel_first=False, SelectiveScan=SelectiveScanCore,
                      cross_selective_scan=cross_selective_scan, force_fp32=None):
       force_fp32 = (self.training and (not self.disable_force32)) if force_fp32 is None else force_fp32
       if not channel_first:
           x = x.permute(0, 3, 1, 2).contiguous()
       if self.ssm_low_rank:
           x = self.in_rank(x)
       x = cross_selective_scan(
           x, self.x_proj_weight, None, self.dt_projs_weight, self.dt_projs_bias,
           self.A_logs, self.Ds,
           out_norm=getattr(self, "out_norm", None),
           out_norm_shape=getattr(self, "out_norm_shape", "v0"),
           delta_softplus=True, force_fp32=force_fp32,
           SelectiveScan=SelectiveScan, ssoflex=self.training,  # output fp32
       )
       if self.ssm_low_rank:
           x = self.out_rank(x)
       return x

   def forward(self, x: torch.Tensor, **kwargs):
       """
       1. in_proj：输入 x 经过 1x1 卷积，若启用门控则分成 x 和 z 两路。

       2. 深度卷积（可选）对 x 做 3x3 深度卷积。

       3. 激活函数（SiLU/GELU）。

       4. 调用 forward_core（实际是 forward_corev2），内部执行 cross_selective_scan。

       5. 如果启用门控，将输出与 z 相乘（门控操作）。

       6. 输出投影和 dropout。
       """
       x = self.in_proj(x)
       if not self.disable_z:
           x, z = x.chunk(2, dim=1)  # (b, d, h, w)
           if not self.disable_z_act:
               z1 = self.act(z)
       if self.d_conv > 0:
           x = self.conv2d(x)  # (b, d, h, w)
       x = self.act(x)
       y = self.forward_core(x, channel_first=(self.d_conv > 1))
       y = y.permute(0, 3, 1, 2).contiguous()
       if not self.disable_z:
           y = y * z1
       out = self.dropout(self.out_proj(y))
       return out

"""
对应论文的 Residual Gated Block（图 3 右侧）。

1. 输入经过 1x1 卷积，通道数翻倍，然后分成两路：x 和 v。

2. x 经过 3x3 深度卷积（带残差连接：dwconv(x) + x）并激活。

3. 然后与 v 逐元素相乘（就是门控机制）。

4. 最后通过 1x1 卷积融合，并 dropout。

作用：增强局部特征和通道间的交互，弥补 SSM 可能忽略的细节。
"""
# 1. 原代码
# class RGBlock(nn.Module):
#     def __init__(self, in_features, hidden_features=None, out_features=None, act_layer=nn.GELU, drop=0.,
#                  channels_first=False):
#         super().__init__()
#         out_features = out_features or in_features
#         hidden_features = hidden_features or in_features
#         hidden_features = int(2 * hidden_features / 3)
#         self.fc1 = nn.Conv2d(in_features, hidden_features * 2, kernel_size=1)
#         self.dwconv = nn.Conv2d(hidden_features, hidden_features, kernel_size=3, stride=1, padding=1, bias=True,
#                                 groups=hidden_features)
#         self.act = act_layer()
#         self.fc2 = nn.Conv2d(hidden_features, out_features, kernel_size=1)
#         self.drop = nn.Dropout(drop)

#     def forward(self, x):
#         x, v = self.fc1(x).chunk(2, dim=1)
#         x = self.act(self.dwconv(x) + x) * v
#         x = self.drop(x)
#         x = self.fc2(x)
#         x = self.drop(x)
#         return x

# 2. DWConv替换为FDConv
class RGBlock(nn.Module):
   def __init__(self, in_features, hidden_features=None, out_features=None, act_layer=nn.GELU, drop=0.,
                channels_first=False):
       super().__init__()
       out_features = out_features or in_features
       hidden_features = hidden_features or in_features
       hidden_features = int(2 * hidden_features / 3)
       self.fc1 = nn.Conv2d(in_features, hidden_features * 2, kernel_size=1)
       self.dwconv = FDConv(
           in_channels=hidden_features,
           out_channels=hidden_features,
           kernel_size=3,
           stride=1,
           padding=1,
           groups=hidden_features,     # 仍然保持深度可分离特性
           bias=True,
           kernel_num=4,               # 论文里小模型常用 4，通道大时可改 8
           # reduction=0.0625,
           # use_fdconv_if_c_gt=16,      # hidden<16 时自动回退 nn.Conv2d，对 T 尺度友好
           # use_fbm_if_k_in=[3],        # 3x3 启用 FBM，针对高频信息调制
       )
       self.act = act_layer()
       self.fc2 = nn.Conv2d(hidden_features, out_features, kernel_size=1)
       self.drop = nn.Dropout(drop)

   def forward(self, x):
       x, v = self.fc1(x).chunk(2, dim=1)
       x = self.act(self.dwconv(x) + x) * v
       x = self.drop(x)
       x = self.fc2(x)
       x = self.drop(x)
       return x

"""
一个轻量级的局部增强模块：

3x3 深度卷积 + BN + 激活 + 1x1 卷积 + 残差连接。

作用是简单提取局部特征，类似于一个微型卷积残差块。
"""
class LSBlock(nn.Module):
   def __init__(self, in_features, hidden_features=None, act_layer=nn.GELU, drop=0):
       super().__init__()
       self.fc1 = nn.Conv2d(in_features, hidden_features, kernel_size=3, padding=3 // 2, groups=hidden_features)
       self.norm = nn.BatchNorm2d(hidden_features)
       self.fc2 = nn.Conv2d(hidden_features, hidden_features, kernel_size=1, padding=0)
       self.act = act_layer()
       self.fc3 = nn.Conv2d(hidden_features, in_features, kernel_size=1, padding=0)
       self.drop = nn.Dropout(drop)

   def forward(self, x):
       input = x
       x = self.fc1(x)
       x = self.norm(x)
       x = self.fc2(x)
       x = self.act(x)
       x = self.fc3(x)
       x = input + self.drop(x)
       return x

"""
ODSSBlock 的变体（论文图 3 的整体结构）。将上述模块组合成一个完整的 Stage 单元。

1. 先通过一个 in_proj（如果输入输出通道数不同）调整通道。

2. 主路径：LSBlock（局部增强）→ 归一化 → SS2D（多个串联）→ 残差相加。

3. 可选 MLP 分支（RGBlock）：再经过一次归一化 + RGBlock + 残差相加。

"""
class XSSBlock(nn.Module):
   def __init__(
           self,
           in_channels: int = 0,
           hidden_dim: int = 0,
           n: int = 1,
           mlp_ratio=4.0,
           drop_path: float = 0,
           norm_layer: Callable[..., torch.nn.Module] = partial(LayerNorm2d, eps=1e-6),
           # =============================
           ssm_d_state: int = 16,
           ssm_ratio=2.0,
           ssm_rank_ratio=2.0,
           ssm_dt_rank: Any = "auto",
           ssm_act_layer=nn.SiLU,
           ssm_conv: int = 3,
           ssm_conv_bias=True,
           ssm_drop_rate: float = 0,
           ssm_init="v0",
           forward_type="v2",
           # =============================
           mlp_act_layer=nn.GELU,
           mlp_drop_rate: float = 0.0,
           # =============================
           use_checkpoint: bool = False,
           post_norm: bool = False,
           **kwargs,
   ):
       super().__init__()

       self.in_proj = nn.Sequential(
           nn.Conv2d(in_channels, hidden_dim, kernel_size=1, stride=1, padding=0, bias=False),
           nn.BatchNorm2d(hidden_dim),
           nn.SiLU()
       ) if in_channels != hidden_dim else nn.Identity()
       self.hidden_dim = hidden_dim
       # ==========SSM============================
       self.norm = norm_layer(hidden_dim)
       self.ss2d = nn.Sequential(*(SS2D(d_model=self.hidden_dim,
                                        d_state=ssm_d_state,
                                        ssm_ratio=ssm_ratio,
                                        ssm_rank_ratio=ssm_rank_ratio,
                                        dt_rank=ssm_dt_rank,
                                        act_layer=ssm_act_layer,
                                        d_conv=ssm_conv,
                                        conv_bias=ssm_conv_bias,
                                        dropout=ssm_drop_rate, ) for _ in range(n)))
       self.drop_path = DropPath(drop_path)
       self.lsblock = LSBlock(hidden_dim, hidden_dim)
       self.mlp_branch = mlp_ratio > 0
       if self.mlp_branch:
           self.norm2 = norm_layer(hidden_dim)
           mlp_hidden_dim = int(hidden_dim * mlp_ratio)
           self.mlp = RGBlock(in_features=hidden_dim, hidden_features=mlp_hidden_dim, act_layer=mlp_act_layer,
                              drop=mlp_drop_rate)

   def forward(self, input):
       input = self.in_proj(input)
       # ====================
       X1 = self.lsblock(input)
       input = input + self.drop_path(self.ss2d(self.norm(X1)))
       # ===================
       if self.mlp_branch:
           input = input + self.drop_path(self.mlp(self.norm2(input)))
       return input


"""
与 XSSBlock 类似，但结构稍有调整（例如在开头加了一个 proj_conv，把 LSBlock 提前）。可能是为了适应不同的网络深度。
"""
class VSSBlock(nn.Module):
   def __init__(
           self,
           in_channels: int = 0,
           hidden_dim: int = 0,
           drop_path: float = 0,
           norm_layer: Callable[..., torch.nn.Module] = partial(LayerNorm2d, eps=1e-6),
           # =============================
           ssm_d_state: int = 16,
           ssm_ratio=2.0,
           ssm_rank_ratio=2.0,
           ssm_dt_rank: Any = "auto",
           ssm_act_layer=nn.SiLU,
           ssm_conv: int = 3,
           ssm_conv_bias=True,
           ssm_drop_rate: float = 0,
           ssm_init="v0",
           forward_type="v2",
           # =============================
           mlp_ratio=4.0,
           mlp_act_layer=nn.GELU,
           mlp_drop_rate: float = 0.0,
           # =============================
           use_checkpoint: bool = False,
           post_norm: bool = False,
           **kwargs,
   ):
       super().__init__()
       self.ssm_branch = ssm_ratio > 0
       self.mlp_branch = mlp_ratio > 0
       self.use_checkpoint = use_checkpoint
       self.post_norm = post_norm

       # proj
       self.proj_conv = nn.Sequential(
           nn.Conv2d(in_channels, hidden_dim, kernel_size=1, stride=1, padding=0, bias=True),
           nn.BatchNorm2d(hidden_dim),
           nn.SiLU()
       )

       if self.ssm_branch:
           self.norm = norm_layer(hidden_dim)
           self.op = SS2D(
               d_model=hidden_dim,
               d_state=ssm_d_state,
               ssm_ratio=ssm_ratio,
               ssm_rank_ratio=ssm_rank_ratio,
               dt_rank=ssm_dt_rank,
               act_layer=ssm_act_layer,
               # ==========================
               d_conv=ssm_conv,
               conv_bias=ssm_conv_bias,
               # ==========================
               dropout=ssm_drop_rate,
               # bias=False,
               # ==========================
               # dt_min=0.001,
               # dt_max=0.1,
               # dt_init="random",
               # dt_scale="random",
               # dt_init_floor=1e-4,
               initialize=ssm_init,
               # ==========================
               forward_type=forward_type,
           )

       self.drop_path = DropPath(drop_path)
       self.lsblock = LSBlock(hidden_dim, hidden_dim)
       if self.mlp_branch:
           self.norm2 = norm_layer(hidden_dim)
           mlp_hidden_dim = int(hidden_dim * mlp_ratio)
           self.mlp = RGBlock(in_features=hidden_dim, hidden_features=mlp_hidden_dim, act_layer=mlp_act_layer,
                              drop=mlp_drop_rate, channels_first=False)

   def forward(self, input: torch.Tensor):
       input = self.proj_conv(input)
       X1 = self.lsblock(input)
       x = input + self.drop_path(self.op(self.norm(X1)))
       if self.mlp_branch:
           x = x + self.drop_path(self.mlp(self.norm2(x)))  # FFN
       return x

"""
对应论文的 Simple Stem：

两个步长为 2 的 3x3 卷积，中间用 GELU 和 BN，最后用 SiLU 激活。

作用是将输入图像（假设 640x640）快速下采样到合适尺寸（如 160x160），同时保留更多细节。
"""
class SimpleStem(nn.Module):
   def __init__(self, inp, embed_dim, ks=3):
       super().__init__()
       self.hidden_dims = embed_dim // 2
       self.conv = nn.Sequential(
           nn.Conv2d(inp, self.hidden_dims, kernel_size=ks, stride=2, padding=autopad(ks, d=1), bias=False),
           nn.BatchNorm2d(self.hidden_dims),
           nn.GELU(),
           nn.Conv2d(self.hidden_dims, embed_dim, kernel_size=ks, stride=2, padding=autopad(ks, d=1), bias=False),
           nn.BatchNorm2d(embed_dim),
           nn.SiLU(),
       )

   def forward(self, x):
       return self.conv(x)

"""
对应论文的 Vision Clue Merge（下采样模块）：

1. 输入特征图 x，将其按空间位置分成四个子图（左上、右上、左下、右下），然后在通道维度拼接，这样通道数变为原来的 4 倍。

2. 再用一个 1x1 卷积将通道数压缩到目标维度（out_dim）。

3. 这样做的好处是保留了原始像素的位置信息，避免使用 stride=2 的卷积导致信息丢失。

相当于把一张大图拆成四个小图，然后把小图叠在一起，再用一个小卷积融合，这样下采样时每个像素都能被保留。
"""
class VisionClueMerge(nn.Module):
   def __init__(self, dim, out_dim):
       super().__init__()
       self.hidden = int(dim * 4)

       self.pw_linear = nn.Sequential(
           nn.Conv2d(self.hidden, out_dim, kernel_size=1, stride=1, padding=0),
           nn.BatchNorm2d(out_dim),
           nn.SiLU()
       )

   def forward(self, x):
       y = torch.cat([
           x[..., ::2, ::2],
           x[..., 1::2, ::2],
           x[..., ::2, 1::2],
           x[..., 1::2, 1::2]
       ], dim=1)
       return self.pw_linear(y)

```





```python
# FDConv.py
"""
FDConv —— Frequency Dynamic Convolution for Dense Image Prediction (CVPR 2025).

本文件是在官方 FDConv_detection/mmdet_custom/FDConv.py 基础上针对 Mamba-YOLO
场景做了如下适配:

1. 移除 mmcv.cnn.CONV_LAYERS 依赖,使其不依赖 mmdet/mmcv 生态。
2. 修复原版在 groups > 1 (包含 depth-wise conv) 时的 reshape 错误,
  所有与"卷积核的输入通道维"相关的张量统一按 in_channels // groups
  构建。

因此本实现可以在 groups=1 / groups>1 / groups==in_channels (DW) 三种情形下
正确前向,适合用于 RGBlock 的 DW-Conv 替换等场景。
"""

import math
import time

import numpy as np
import torch
import torch.autograd
import torch.nn as nn
import torch.nn.functional as F
from torch import Tensor
from torch.utils.checkpoint import checkpoint
from timm.models.layers import trunc_normal_


# ----------------------------------------------------------------------------
# 基础激活模块
# ----------------------------------------------------------------------------
class StarReLU(nn.Module):
   """StarReLU 激活: s * relu(x) ** 2 + b。

   相比普通 ReLU 具有可学习的尺度与偏置,常用于注意力分支的非线性变换。
   """

   def __init__(self, scale_value=1.0, bias_value=0.0,
                scale_learnable=True, bias_learnable=True,
                mode=None, inplace=False):
       """初始化 StarReLU 的尺度/偏置参数。"""
       super().__init__()
       self.inplace = inplace
       self.relu = nn.ReLU(inplace=inplace)
       self.scale = nn.Parameter(scale_value * torch.ones(1), requires_grad=scale_learnable)
       self.bias = nn.Parameter(bias_value * torch.ones(1), requires_grad=bias_learnable)

   def forward(self, x):
       """前向: 对输入应用 StarReLU 非线性。"""
       return self.scale * self.relu(x) ** 2 + self.bias


# ----------------------------------------------------------------------------
# KSM Global: 基于全局上下文生成 channel / filter / spatial / kernel 四类注意力
# ----------------------------------------------------------------------------
class KernelSpatialModulation_Global(nn.Module):
   """KSM 的全局分支,按 ODConv 范式输出多路注意力。

   注意:
       - in_planes 表示 "卷积核输入通道维" 的长度 (= in_channels // groups),
         用于 channel_fc 的输出通道数,使其与 kernel 权重的 cin 轴对齐。
       - in_planes_for_input 表示 "输入特征图的真实通道数" (= in_channels),
         用于 fc 的输入通道数。
       - groups: 当 in_planes == groups == out_planes 时视为 depth-wise conv,
         此时 filter 注意力退化为 skip。
   """

   def __init__(self, in_planes, out_planes, kernel_size, groups=1, reduction=0.0625,
                kernel_num=4, min_channel=16, temp=1.0, kernel_temp=None,
                kernel_att_init='dyconv_as_extra', att_multi=2.0, ksm_only_kernel_att=False,
                att_grid=1, stride=1, spatial_freq_decompose=False, act_type='sigmoid',
                in_planes_for_input=None):
       """构造 KSM_Global 所需要的各条注意力分支。"""
       super().__init__()
       # 若未指定输入特征图通道数,则默认与 kernel cin 维一致(即 groups=1 情形)
       if in_planes_for_input is None:
           in_planes_for_input = in_planes

       attention_channel = max(int(in_planes * reduction), min_channel)
       self.act_type = act_type
       self.kernel_size = kernel_size
       self.kernel_num = kernel_num

       self.temperature = temp
       self.kernel_temp = kernel_temp

       self.ksm_only_kernel_att = ksm_only_kernel_att
       self.kernel_att_init = kernel_att_init
       self.att_multi = att_multi

       self.avgpool = nn.AdaptiveAvgPool2d(1)
       self.att_grid = att_grid
       # 注意: fc 接受原始输入特征(通道数 = in_planes_for_input)
       self.fc = nn.Conv2d(in_planes_for_input, attention_channel, 1, bias=False)
       self.bn = nn.BatchNorm2d(attention_channel)
       self.relu = StarReLU()

       self.spatial_freq_decompose = spatial_freq_decompose

       # channel 注意力: 其输出维度对齐 kernel 的 cin 轴(=in_planes)
       if ksm_only_kernel_att:
           self.func_channel = self.skip
       else:
           if spatial_freq_decompose:
               self.channel_fc = nn.Conv2d(
                   attention_channel,
                   in_planes * 2 if self.kernel_size > 1 else in_planes,
                   1, bias=True,
               )
           else:
               self.channel_fc = nn.Conv2d(attention_channel, in_planes, 1, bias=True)
           self.func_channel = self.get_channel_attention

       # filter 注意力: depth-wise conv 场景下退化
       if (in_planes == groups and in_planes == out_planes) or self.ksm_only_kernel_att:
           self.func_filter = self.skip
       else:
           if spatial_freq_decompose:
               self.filter_fc = nn.Conv2d(attention_channel, out_planes * 2, 1, stride=stride, bias=True)
           else:
               self.filter_fc = nn.Conv2d(attention_channel, out_planes, 1, stride=stride, bias=True)
           self.func_filter = self.get_filter_attention

       # spatial 注意力: 1x1 卷积场景下退化
       if kernel_size == 1 or self.ksm_only_kernel_att:
           self.func_spatial = self.skip
       else:
           self.spatial_fc = nn.Conv2d(attention_channel, kernel_size * kernel_size, 1, bias=True)
           self.func_spatial = self.get_spatial_attention

       # kernel 注意力: 在 kernel_num 个基函数间做加权
       if kernel_num == 1:
           self.func_kernel = self.skip
       else:
           self.kernel_fc = nn.Conv2d(attention_channel, kernel_num, 1, bias=True)
           self.func_kernel = self.get_kernel_attention

       self._initialize_weights()

   def _initialize_weights(self):
       """对内部卷积/BN 做合适的初始化。"""
       for m in self.modules():
           if isinstance(m, nn.Conv2d):
               nn.init.kaiming_normal_(m.weight, mode='fan_out', nonlinearity='relu')
               if m.bias is not None:
                   nn.init.constant_(m.bias, 0)
           if isinstance(m, nn.BatchNorm2d):
               nn.init.constant_(m.weight, 1)
               nn.init.constant_(m.bias, 0)

       if hasattr(self, 'spatial_fc') and isinstance(self.spatial_fc, nn.Conv2d):
           nn.init.normal_(self.spatial_fc.weight, std=1e-6)
       if hasattr(self, 'filter_fc') and isinstance(self.filter_fc, nn.Conv2d):
           nn.init.normal_(self.filter_fc.weight, std=1e-6)
       if hasattr(self, 'kernel_fc') and isinstance(self.kernel_fc, nn.Conv2d):
           nn.init.normal_(self.kernel_fc.weight, std=1e-6)
       if hasattr(self, 'channel_fc') and isinstance(self.channel_fc, nn.Conv2d):
           nn.init.normal_(self.channel_fc.weight, std=1e-6)

   def update_temperature(self, temperature):
       """动态更新注意力的 softmax/sigmoid 温度。"""
       self.temperature = temperature

   @staticmethod
   def skip(_):
       """占位函数,返回常量 1.0,用于禁用某一路注意力。"""
       return 1.0

   def get_channel_attention(self, x):
       """根据全局特征生成 channel 注意力,形状 (b, 1, 1, cin_g, 1, 1)。"""
       if self.act_type == 'sigmoid':
           channel_attention = torch.sigmoid(
               self.channel_fc(x).view(x.size(0), 1, 1, -1, x.size(-2), x.size(-1)) / self.temperature
           ) * self.att_multi
       elif self.act_type == 'tanh':
           channel_attention = 1 + torch.tanh_(
               self.channel_fc(x).view(x.size(0), 1, 1, -1, x.size(-2), x.size(-1)) / self.temperature
           )
       else:
           raise NotImplementedError
       return channel_attention

   def get_filter_attention(self, x):
       """根据全局特征生成 filter 注意力,形状 (b, 1, cout, 1, 1, 1)。"""
       if self.act_type == 'sigmoid':
           filter_attention = torch.sigmoid(
               self.filter_fc(x).view(x.size(0), 1, -1, 1, x.size(-2), x.size(-1)) / self.temperature
           ) * self.att_multi
       elif self.act_type == 'tanh':
           filter_attention = 1 + torch.tanh_(
               self.filter_fc(x).view(x.size(0), 1, -1, 1, x.size(-2), x.size(-1)) / self.temperature
           )
       else:
           raise NotImplementedError
       return filter_attention

   def get_spatial_attention(self, x):
       """根据全局特征生成 spatial 注意力,形状 (b, 1, 1, 1, k, k)。"""
       spatial_attention = self.spatial_fc(x).view(x.size(0), 1, 1, 1, self.kernel_size, self.kernel_size)
       if self.act_type == 'sigmoid':
           spatial_attention = torch.sigmoid(spatial_attention / self.temperature) * self.att_multi
       elif self.act_type == 'tanh':
           spatial_attention = 1 + torch.tanh_(spatial_attention / self.temperature)
       else:
           raise NotImplementedError
       return spatial_attention

   def get_kernel_attention(self, x):
       """根据全局特征生成 kernel 基函数权重,形状 (b, kernel_num, 1, 1, 1, 1)。"""
       kernel_attention = self.kernel_fc(x).view(x.size(0), -1, 1, 1, 1, 1)
       if self.act_type == 'softmax':
           kernel_attention = F.softmax(kernel_attention / self.kernel_temp, dim=1)
       elif self.act_type == 'sigmoid':
           kernel_attention = torch.sigmoid(kernel_attention / self.kernel_temp) * 2 / kernel_attention.size(1)
       elif self.act_type == 'tanh':
           kernel_attention = (1 + torch.tanh(kernel_attention / self.kernel_temp)) / kernel_attention.size(1)
       else:
           raise NotImplementedError
       return kernel_attention

   def forward(self, x, use_checkpoint=False):
       """前向: 可选地走 checkpoint 以节省显存。"""
       if use_checkpoint:
           return checkpoint(self._forward, x)
       return self._forward(x)

   def _forward(self, x):
       """实际前向: 先投影到注意力空间,再分别输出四路注意力。"""
       avg_x = self.relu(self.bn(self.fc(x)))
       return (
           self.func_channel(avg_x),
           self.func_filter(avg_x),
           self.func_spatial(avg_x),
           self.func_kernel(avg_x),
       )


# ----------------------------------------------------------------------------
# KSM Local: 借助 1D 卷积建模跨通道交互,生成逐通道逐空间的细粒度注意力
# ----------------------------------------------------------------------------
class KernelSpatialModulation_Local(nn.Module):
   """ECA 风格的局部 KSM 模块。

   Args:
       channel: 输入到本模块的通道数(对应 FDConv 的 in_channels_per_group)。
       kernel_num: 基函数数量。
       out_n: 每个 cin 位置要输出的展开维度(通常 = cout * k * k)。
       k_size: 1D 卷积核大小,会按 channel 自动调整。
       use_global: 是否额外启用 FFT 全局调制。
   """

   def __init__(self, channel=None, kernel_num=1, out_n=1, k_size=3, use_global=False):
       """构造 KSM_Local 内部的 Conv1d / LayerNorm 等。"""
       super().__init__()
       self.kn = kernel_num
       self.out_n = out_n
       self.channel = channel
       if channel is not None:
           # 根据通道数自适应 1D 卷积核大小,最小为 1
           k_size = max(1, round((math.log2(max(channel, 2)) / 2) + 0.5) // 2 * 2 + 1)
       self.conv = nn.Conv1d(1, kernel_num * out_n, kernel_size=k_size,
                             padding=(k_size - 1) // 2, bias=False)
       nn.init.constant_(self.conv.weight, 1e-6)
       self.use_global = use_global
       if self.use_global:
           self.complex_weight = nn.Parameter(
               torch.randn(1, self.channel // 2 + 1, 2, dtype=torch.float32) * 1e-6
           )
       self.norm = nn.LayerNorm(self.channel)

   def forward(self, x, x_std=None):
       """前向: 输入形状 (b, c, 1, 1),输出形状 (b, kn, c, out_n)。"""
       x = x.squeeze(-1).transpose(-1, -2)  # (b, 1, c)
       b, _, c = x.shape
       if self.use_global:
           x_rfft = torch.fft.rfft(x.float(), dim=-1)
           x_real = x_rfft.real * self.complex_weight[..., 0][None]
           x_imag = x_rfft.imag * self.complex_weight[..., 1][None]
           x = x + torch.fft.irfft(
               torch.view_as_complex(torch.stack([x_real, x_imag], dim=-1)), dim=-1,
           )
       x = self.norm(x)
       att_logit = self.conv(x)
       att_logit = att_logit.reshape(x.size(0), self.kn, self.out_n, c)  # (b, kn, out_n, c)
       att_logit = att_logit.permute(0, 1, 3, 2)  # (b, kn, c, out_n)
       return att_logit


# ----------------------------------------------------------------------------
# FBM: 频带调制模块
# ----------------------------------------------------------------------------
class FrequencyBandModulation(nn.Module):
   """FBM: 将输入特征分解为若干频带,并按频带动态加权。"""

   def __init__(self,
                in_channels,
                k_list=[2],
                lowfreq_att=False,
                fs_feat='feat',
                act='sigmoid',
                spatial='conv',
                spatial_group=1,
                spatial_kernel=3,
                init='zero',
                max_size=(64, 64),
                **kwargs):
       """构造 FBM 的频带掩码与注意力卷积。"""
       super().__init__()
       self.k_list = k_list
       self.lowfreq_att = lowfreq_att
       self.in_channels = in_channels
       self.fs_feat = fs_feat
       self.act = act

       if spatial_group > 64:
           spatial_group = in_channels
       self.spatial_group = spatial_group

       if spatial == 'conv':
           self.freq_weight_conv_list = nn.ModuleList()
           _n = len(k_list)
           if lowfreq_att:
               _n += 1
           for i in range(_n):
               freq_weight_conv = nn.Conv2d(
                   in_channels=in_channels,
                   out_channels=self.spatial_group,
                   stride=1,
                   kernel_size=spatial_kernel,
                   groups=self.spatial_group,
                   padding=spatial_kernel // 2,
                   bias=True,
               )
               if init == 'zero':
                   nn.init.normal_(freq_weight_conv.weight, std=1e-6)
                   if freq_weight_conv.bias is not None:
                       freq_weight_conv.bias.data.zero_()
               self.freq_weight_conv_list.append(freq_weight_conv)
       else:
           raise NotImplementedError

       # 预计算多个频带的二值 mask 以加速训练
       self.register_buffer('cached_masks', self._precompute_masks(max_size, k_list), persistent=False)

   def _precompute_masks(self, max_size, k_list):
       """一次性构造各频带的低通掩码,后续插值复用。"""
       max_h, max_w = max_size
       _, freq_indices = get_fft2freq(d1=max_h, d2=max_w, use_rfft=True)
       freq_indices = freq_indices.abs().max(dim=-1, keepdims=False)[0]

       masks = []
       for freq in k_list:
           mask = freq_indices < 0.5 / freq + 1e-8
           masks.append(mask)

       return torch.stack(masks, dim=0).unsqueeze(1)

   def sp_act(self, freq_weight):
       """将频带注意力 logit 通过指定激活函数映射到 (0, 2) 区间。"""
       if self.act == 'sigmoid':
           return freq_weight.sigmoid() * 2
       elif self.act == 'tanh':
           return 1 + freq_weight.tanh()
       elif self.act == 'softmax':
           return freq_weight.softmax(dim=1) * freq_weight.shape[1]
       else:
           raise NotImplementedError

   def forward(self, x, att_feat=None):
       """前向: 对不同频带分别做注意力调制后求和重建特征。"""
       if att_feat is None:
           att_feat = x

       x_list = []
       x = x.to(torch.float32)
       pre_x = x.clone()
       b, _, h, w = x.shape

       x_fft = torch.fft.rfft2(x, norm='ortho')

       freq_h, freq_w = h, w // 2 + 1
       current_masks = F.interpolate(self.cached_masks.float(), size=(freq_h, freq_w), mode='nearest')

       for idx, freq in enumerate(self.k_list):
           mask = current_masks[idx]
           low_part = torch.fft.irfft2(x_fft * mask, s=(h, w), norm='ortho')
           high_part = pre_x - low_part
           pre_x = low_part

           freq_weight = self.freq_weight_conv_list[idx](att_feat)
           freq_weight = self.sp_act(freq_weight)

           tmp = freq_weight.reshape(b, self.spatial_group, -1, h, w) * \
                 high_part.reshape(b, self.spatial_group, -1, h, w)
           x_list.append(tmp.reshape(b, -1, h, w))

       if self.lowfreq_att:
           freq_weight = self.freq_weight_conv_list[len(self.k_list)](att_feat)
           freq_weight = self.sp_act(freq_weight)
           tmp = freq_weight.reshape(b, self.spatial_group, -1, h, w) * \
                 pre_x.reshape(b, self.spatial_group, -1, h, w)
           x_list.append(tmp.reshape(b, -1, h, w))
       else:
           x_list.append(pre_x)

       return sum(x_list)


def get_fft2freq(d1, d2, use_rfft=False):
   """计算 2D (r)fft 后各频点的坐标,并按到原点的距离排序。"""
   freq_h = torch.fft.fftfreq(d1)
   if use_rfft:
       freq_w = torch.fft.rfftfreq(d2)
   else:
       freq_w = torch.fft.fftfreq(d2)

   freq_hw = torch.stack(torch.meshgrid(freq_h, freq_w), dim=-1)
   dist = torch.norm(freq_hw, dim=-1)
   sorted_dist, indices = torch.sort(dist.view(-1))

   if use_rfft:
       d2 = d2 // 2 + 1
   sorted_coords = torch.stack([indices // d2, indices % d2], dim=-1)
   return sorted_coords.permute(1, 0), freq_hw


# ----------------------------------------------------------------------------
# FDConv: 频域动态卷积主模块
# ----------------------------------------------------------------------------
class FDConv(nn.Conv2d):
   """Frequency Dynamic Convolution(group-aware 版本)。

   - 继承 nn.Conv2d,因此构造签名与 API 完全兼容普通卷积。
   - 当 min(in, out) <= use_fdconv_if_c_gt 或 kernel_size 不在
     use_fdconv_if_k_in 中时,forward 会直接退化为 nn.Conv2d 标准前向。
   - 本版本修复了原实现在 groups > 1 时 kernel cin 轴 reshape 错误的 bug,
     所有涉及到"卷积核输入通道维"的位置均改用 in_channels // groups。
   """

   def __init__(self,
                *args,
                reduction=0.0625,
                kernel_num=4,
                use_fdconv_if_c_gt=16,
                use_fdconv_if_k_in=[1, 3],
                use_fdconv_if_stride_in=[1],
                use_fbm_if_k_in=[3],
                use_fbm_for_stride=False,
                kernel_temp=1.0,
                temp=None,
                att_multi=2.0,
                param_ratio=1,
                param_reduction=1.0,
                ksm_only_kernel_att=False,
                att_grid=1,
                use_ksm_local=True,
                ksm_local_act='sigmoid',
                ksm_global_act='sigmoid',
                spatial_freq_decompose=False,
                convert_param=True,
                linear_mode=False,
                fbm_cfg={
                    'k_list': [2, 4, 8],
                    'lowfreq_att': False,
                    'fs_feat': 'feat',
                    'act': 'sigmoid',
                    'spatial': 'conv',
                    'spatial_group': 1,
                    'spatial_kernel': 3,
                    'init': 'zero',
                    'global_selection': False,
                },
                **kwargs):
       """初始化 FDConv 的超参、KSM/FBM 子模块以及 DFT 权重。"""
       super().__init__(*args, **kwargs)
       self.use_fdconv_if_c_gt = use_fdconv_if_c_gt
       self.use_fdconv_if_k_in = use_fdconv_if_k_in
       self.use_fdconv_if_stride_in = use_fdconv_if_stride_in
       self.kernel_num = kernel_num
       self.param_ratio = param_ratio
       self.param_reduction = param_reduction
       self.use_ksm_local = use_ksm_local
       self.att_multi = att_multi
       self.spatial_freq_decompose = spatial_freq_decompose
       self.use_fbm_if_k_in = use_fbm_if_k_in

       self.ksm_local_act = ksm_local_act
       self.ksm_global_act = ksm_global_act
       assert self.ksm_local_act in ['sigmoid', 'tanh']
       assert self.ksm_global_act in ['softmax', 'sigmoid', 'tanh']

       # group-aware: 卷积核真正的输入通道维
       self.in_channels_per_group = self.in_channels // self.groups
       # 防止 in_channels_per_group==1 导致 min(...)//2 == 0 的除 0 问题
       self._denom = max(1, min(self.out_channels, self.in_channels_per_group) // 2)

       # kernel_num & kernel_temp 的自适应
       if self.kernel_num is None:
           self.kernel_num = self.out_channels // 2
           kernel_temp = math.sqrt(self.kernel_num * self.param_ratio)
       if temp is None:
           temp = kernel_temp

       # 通道/核尺寸不满足条件时,直接走父类的普通 Conv2d
       if min(self.in_channels, self.out_channels) <= self.use_fdconv_if_c_gt \
               or self.kernel_size[0] not in self.use_fdconv_if_k_in:
           return

       print('*** FDConv kernel_num:', self.kernel_num,
             '| in_channels_per_group:', self.in_channels_per_group,
             '| groups:', self.groups)

       # alpha 与 _denom 配合,保证与 dft_weight 归一化系数相互抵消
       self.alpha = self._denom * self.kernel_num * self.param_ratio / param_reduction

       # ---- KSM Global ----
       # in_planes(=in_channels_per_group)用于 channel_fc 的输出维度对齐 kernel cin 轴;
       # in_planes_for_input(=in_channels)用于 fc 输入的真实特征通道数。
       self.KSM_Global = KernelSpatialModulation_Global(
           in_planes=self.in_channels_per_group,
           out_planes=self.out_channels,
           kernel_size=self.kernel_size[0],
           groups=self.groups,
           temp=temp,
           kernel_temp=kernel_temp,
           reduction=reduction,
           kernel_num=self.kernel_num * self.param_ratio,
           kernel_att_init=None,
           att_multi=att_multi,
           ksm_only_kernel_att=ksm_only_kernel_att,
           act_type=self.ksm_global_act,
           att_grid=att_grid,
           stride=self.stride,
           spatial_freq_decompose=spatial_freq_decompose,
           in_planes_for_input=self.in_channels,
       )

       # ---- FBM ----
       if self.kernel_size[0] in use_fbm_if_k_in or (use_fbm_for_stride and self.stride[0] > 1):
           self.FBM = FrequencyBandModulation(self.in_channels, **fbm_cfg)

       # ---- KSM Local ----
       # group > 1 时 kernel cin=1(DW),此时 LayerNorm(1) 退化为常数输出,
       # 局部注意力失去意义,故直接关闭。
       if self.use_ksm_local and self.in_channels_per_group > 1:
           self.KSM_Local = KernelSpatialModulation_Local(
               channel=self.in_channels_per_group,
               kernel_num=1,
               out_n=int(self.out_channels * self.kernel_size[0] * self.kernel_size[1]),
           )
       else:
           self.use_ksm_local = False

       self.linear_mode = linear_mode
       self.convert2dftweight(convert_param)

   def convert2dftweight(self, convert_param):
       """把 spatial-domain 权重转为 DFT 系数,并(可选)注册为可训练参数。"""
       d1 = self.out_channels
       d2 = self.in_channels_per_group  # 真正的 kernel cin 维
       k1, k2 = self.kernel_size[0], self.kernel_size[1]

       freq_indices, _ = get_fft2freq(d1 * k1, d2 * k2, use_rfft=True)
       # weight 原始形状 (d1, d2, k1, k2),先 permute 为 (d1, k1, d2, k2) 再展开
       weight = self.weight.permute(0, 2, 1, 3).reshape(d1 * k1, d2 * k2)
       weight_rfft = torch.fft.rfft2(weight, dim=(0, 1))

       if self.param_reduction < 1:
           num_to_keep = max(1, int(freq_indices.size(1) * self.param_reduction))
           # 保证能整除 kernel_num,方便后续 reshape
           num_to_keep = max(self.kernel_num, (num_to_keep // self.kernel_num) * self.kernel_num)
           freq_indices = freq_indices[:, :num_to_keep]
           weight_rfft = torch.stack([weight_rfft.real, weight_rfft.imag], dim=-1)
           weight_rfft = weight_rfft[freq_indices[0, :], freq_indices[1, :]]
           weight_rfft = weight_rfft.reshape(-1, 2)[None, ].repeat(self.param_ratio, 1, 1) / self._denom
       else:
           weight_rfft = torch.stack([weight_rfft.real, weight_rfft.imag], dim=-1)[None, ].repeat(
               self.param_ratio, 1, 1, 1,
           ) / self._denom

       if convert_param:
           self.dft_weight = nn.Parameter(weight_rfft, requires_grad=True)
           del self.weight
       else:
           if self.linear_mode:
               assert self.kernel_size[0] == 1 and self.kernel_size[1] == 1
               self.weight = torch.nn.Parameter(self.weight.squeeze(), requires_grad=True)

       indices = []
       for i in range(self.param_ratio):
           indices.append(freq_indices.reshape(2, self.kernel_num, -1))
       self.register_buffer('indices', torch.stack(indices, dim=0), persistent=False)

   def get_FDW(self):
       """未转换到频域时,按当前 spatial weight 即时计算对应的 DFT 系数。"""
       d1 = self.out_channels
       d2 = self.in_channels_per_group
       k1, k2 = self.kernel_size[0], self.kernel_size[1]
       weight = self.weight.reshape(d1, d2, k1, k2).permute(0, 2, 1, 3).reshape(d1 * k1, d2 * k2)
       weight_rfft = torch.fft.rfft2(weight, dim=(0, 1)).contiguous()
       weight_rfft = torch.stack([weight_rfft.real, weight_rfft.imag], dim=-1)[None, ].repeat(
           self.param_ratio, 1, 1, 1,
       ) / self._denom
       return weight_rfft

   def forward(self, x):
       """前向: 根据通道/核尺寸条件,选择 FDConv 动态核卷积或退回普通卷积。"""
       if min(self.in_channels, self.out_channels) <= self.use_fdconv_if_c_gt \
               or self.kernel_size[0] not in self.use_fdconv_if_k_in:
           return super().forward(x)

       cin_g = self.in_channels_per_group
       k1, k2 = self.kernel_size[0], self.kernel_size[1]

       # 全局上下文
       global_x = F.adaptive_avg_pool2d(x, 1)
       channel_attention, filter_attention, spatial_attention, kernel_attention = self.KSM_Global(global_x)

       # 细粒度局部注意力(仅 in_channels_per_group > 1 时启用)
       if self.use_ksm_local:
           if self.groups > 1:
               # (b, C, 1, 1) -> (b, groups, cin_g, 1, 1) -> 组内平均 -> (b, cin_g, 1, 1)
               gx_for_local = global_x.view(x.size(0), self.groups, cin_g, 1, 1).mean(dim=1)
           else:
               gx_for_local = global_x

           hr_att_logit = self.KSM_Local(gx_for_local)  # (b, 1, cin_g, cout*k*k)
           hr_att_logit = hr_att_logit.reshape(x.size(0), 1, cin_g, self.out_channels, k1, k2)
           hr_att_logit = hr_att_logit.permute(0, 1, 3, 2, 4, 5)  # (b, 1, cout, cin_g, k, k)
           if self.ksm_local_act == 'sigmoid':
               hr_att = hr_att_logit.sigmoid() * self.att_multi
           elif self.ksm_local_act == 'tanh':
               hr_att = 1 + hr_att_logit.tanh()
           else:
               raise NotImplementedError
       else:
           hr_att = 1

       batch_size, in_planes, height, width = x.size()
       b = batch_size

       # DFT_map: 按 cin_g 构建,与 FDW / indices 对齐;
       # view_as_complex 要求 float32/float64,始终使用 float32 以兼容 AMP。
       DFT_map = torch.zeros(
           (b, self.out_channels * k1, cin_g * k2 // 2 + 1, 2),
           device=x.device, dtype=torch.float32,
       )
       kernel_attention = kernel_attention.reshape(b, self.param_ratio, self.kernel_num, -1)

       dft_weight = self.dft_weight if hasattr(self, 'dft_weight') else self.get_FDW()

       for i in range(self.param_ratio):
           indices = self.indices[i]
           if self.param_reduction < 1:
               w = dft_weight[i].reshape(self.kernel_num, -1, 2)[None]
               DFT_map[:, indices[0, :, :], indices[1, :, :]] += torch.stack(
                   [w[..., 0] * kernel_attention[:, i], w[..., 1] * kernel_attention[:, i]], dim=-1,
               )
           else:
               w = dft_weight[i][indices[0, :, :], indices[1, :, :]][None] * self.alpha
               DFT_map[:, indices[0, :, :], indices[1, :, :]] += torch.stack(
                   [w[..., 0] * kernel_attention[:, i], w[..., 1] * kernel_attention[:, i]], dim=-1,
               )

       # 还原为 (b, 1, cout, cin_g, k1, k2)
       adaptive_weights = torch.fft.irfft2(
           torch.view_as_complex(DFT_map), dim=(1, 2),
       ).reshape(batch_size, 1, self.out_channels, k1, cin_g, k2)
       adaptive_weights = adaptive_weights.permute(0, 1, 2, 4, 3, 5)

       # FBM 对输入做频带调制
       if hasattr(self, 'FBM'):
           x = self.FBM(x)

       # 聚合所有注意力,广播相乘得到每个 batch 自己的动态 kernel
       # channel_attention: (b, 1, 1, cin_g, 1, 1) (skip 时为 1.0)
       # filter_attention : (b, 1, cout, 1, 1, 1) (skip 时为 1.0)
       # spatial_attention: (b, 1, 1, 1, k, k)   (skip 时为 1.0)
       # hr_att           : (b, 1, cout, cin_g, k, k) (或 1.0)
       aggregate_weight = spatial_attention * channel_attention * filter_attention * adaptive_weights * hr_att
       aggregate_weight = torch.sum(aggregate_weight, dim=1)  # (b, cout, cin_g, k, k)
       aggregate_weight = aggregate_weight.reshape(
           batch_size * self.out_channels, cin_g, k1, k2,
       )

       # 将 batch 维合并到 groups 维以实现 per-sample dynamic conv
       x = x.reshape(1, -1, height, width)
       output = F.conv2d(
           x, weight=aggregate_weight, bias=None,
           stride=self.stride, padding=self.padding,
           dilation=self.dilation, groups=self.groups * batch_size,
       )
       output = output.view(batch_size, self.out_channels, output.size(-2), output.size(-1))

       if self.bias is not None:
           output = output + self.bias.view(1, -1, 1, 1)
       return output

   def profile_module(self, input: Tensor, *args, **kwargs):
       """保留自 mmdet 原实现的占位接口,供外部分析工具调用。"""
       b_sz, c, h, w = input.shape
       seq_len = h * w

       p_ff, m_ff = 0, 5 * b_sz * seq_len * int(math.log(seq_len)) * c
       params = macs = getattr(self, 'hidden_size', c) * 2
       macs = macs * b_sz * seq_len

       return input, params, macs + m_ff


if __name__ == '__main__':
   # 快速自测: groups=1 / groups=4 / depth-wise 三种情形都能正常前向
   for g in [1, 4, 64]:
       m = FDConv(in_channels=64, out_channels=64, kernel_size=3, padding=1,
                  groups=g, bias=True)
       y = m(torch.randn(2, 64, 16, 16))
       print(f'groups={g}, out shape: {y.shape}')

```



```python
# test_fdconv_mbyolo.py
import torch
from ultralytics.nn.modules.mamba_yolo import RGBlock, SimpleStem, XSSBlock, VSSBlock

x = torch.randn(2, 128, 32, 32).cuda()

rg = RGBlock(in_features=128, hidden_features=256).cuda()
print('RGBlock ok:', rg(x).shape)

# stem = SimpleStem(3, 128).cuda()
# print('Stem ok:', stem(torch.randn(2, 3, 640, 640).cuda()).shape)

# xss = XSSBlock(in_channels=128, hidden_dim=256, n=1).cuda()
# print('XSSBlock ok:', xss(x).shape)

# vss = VSSBlock(in_channels=128, hidden_dim=256).cuda()
# print('VSSBlock ok:', vss(x).shape)
```



运行`python test_fdconv_mbyolo.py`报错：

```sh
(mambayolo) huangyh@user-ThinkStation-P920:~/Mamba-YOLO/tests$ python test_fdconv_mbyolo.py 
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/timm/models/layers/__init__.py:49: FutureWarning: Importing from timm.models.layers is deprecated, please import via timm.layers
 warnings.warn(f"Importing from {__name__} is deprecated, please import via timm.layers", FutureWarning)
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/functional.py:512: UserWarning: torch.meshgrid: in an upcoming release, it will be required to pass the indexing argument. (Triggered internally at ../aten/src/ATen/native/TensorShape.cpp:3587.)
 return _VF.meshgrid(tensors, **kwargs)  # type: ignore[attr-defined]
Traceback (most recent call last):
 File "/home/huangyh/Mamba-YOLO/tests/test_fdconv_mbyolo.py", line 7, in <module>
   print('RGBlock ok:', rg(x).shape)
                        ^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/mamba_yolo.py", line 284, in forward
   x = self.act(self.dwconv(x) + x) * v
                ^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/FDConv.py", line 668, in forward
   ).reshape(batch_size, 1, self.out_channels, k1, cin_g, k2)
     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
RuntimeError: shape '[2, 1, 170, 3, 1, 3]' is invalid for input of size 2040
```

帮我分析一下问题出在哪里？







错误原因定位在 FDConv.forward 方法中调用 torch.fft.irfft2 时没有指定输出信号的空间尺寸 s。

具体分析：

在深度卷积（groups=hidden_features）场景下，in_channels_per_group = 1，kernel_size = 3，因此 cin_g * k2 = 1 * 3 = 3。

构造的 DFT_map 复数张量形状为 (batch, out_channels * k1, cin_g * k2 // 2 + 1) = (2, 510, 2)。

torch.fft.irfft2 在不提供 s 参数时，会根据输入复数张量的最后两维推断输出尺寸：高度不变（510），宽度推断为 2 * (2 - 1) = 2，于是输出张量形状为 (2, 510, 2)，总元素数 2040。

而后续 reshape 期望的形状 (2, 1, 170, 3, 1, 3) 需要总元素数 3060，因此报错形状不匹配。

解决方案：
在 irfft2 调用中显式指定 s=(self.out_channels * k1, cin_g * k2)，使其恢复出正确的空间尺寸。

修改 FDConv.py 第 668 行附近代码为：

python

adaptive_weights = torch.fft.irfft2(
    torch.view_as_complex(DFT_map), 
    dim=(1, 2), 
    s=(self.out_channels * k1, cin_g * k2)
).reshape(batch_size, 1, self.out_channels, k1, cin_g, k2)



同时建议检查 get_FDW 方法中的 irfft2 调用是否也存在类似问题（若后续使用该方法时遇到相同错误，亦需补全 s 参数）。













修改 FDConv.py 第 668 行附近代码之后再跑代码任报错：
(mambayolo) huangyh@user-ThinkStation-P920:~/Mamba-YOLO/tests$ python test_fdconv_mbyolo.py 
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/timm/models/layers/__init__.py:49: FutureWarning: Importing from timm.models.layers is deprecated, please import via timm.layers
 warnings.warn(f"Importing from {__name__} is deprecated, please import via timm.layers", FutureWarning)
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/functional.py:512: UserWarning: torch.meshgrid: in an upcoming release, it will be required to pass the indexing argument. (Triggered internally at ../aten/src/ATen/native/TensorShape.cpp:3587.)
 return _VF.meshgrid(tensors, **kwargs)  # type: ignore[attr-defined]
Traceback (most recent call last):
 File "/home/huangyh/Mamba-YOLO/tests/test_fdconv_mbyolo.py", line 7, in <module>
   print('RGBlock ok:', rg(x).shape)
                        ^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/mamba_yolo.py", line 284, in forward
   x = self.act(self.dwconv(x) + x) * v
                ^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/FDConv.py", line 682, in forward
   aggregate_weight = aggregate_weight.reshape(
                      ^^^^^^^^^^^^^^^^^^^^^^^^^
RuntimeError: shape '[340, 1, 3, 3]' is invalid for input of size 9180















问题已定位：FDConv.forward 中 adaptive_weights 的维度顺序与其他注意力张量不一致，导致广播后形状混乱，进而引发 reshape 错误。

错误分析

在深度可分离卷积（groups=in_channels）下，in_channels_per_group = 1，kernel_size = 3。

所有注意力张量经广播后应统一为 6 维格式：(batch, kernel_num, out_channels, in_per_group, kH, kW)。

但 adaptive_weights 当前的维度顺序为 (b, 1, out, kH, in_per_group, kW)，而 hr_att 等其他注意力的 in_per_group 维度在 kH 之前，导致乘法时维度错位，最终 aggregate_weight 的形状异常（例如 cin_g 被错误扩展为 3），元素总数变为预期的 3 倍（3060 → 9180）。

修复方案

修改 FDConv.py 中 forward 函数的相关代码段，确保 adaptive_weights 的维度顺序与 hr_att 保持一致。

原代码（约第 668 行）：

python

adaptive_weights = torch.fft.irfft2(
    torch.view_as_complex(DFT_map), dim=(1, 2)
).reshape(batch_size, 1, self.out_channels, k1, cin_g, k2)



修改为：

python

adaptive_weights = torch.fft.irfft2(
    torch.view_as_complex(DFT_map),
    dim=(1, 2),
    s=(self.out_channels * k1, cin_g * k2)        # 显式指定输出尺寸
).reshape(batch_size, 1, self.out_channels, k1, cin_g, k2)
adaptive_weights = adaptive_weights.permute(0, 1, 2, 4, 3, 5)   # 交换 k1 与 cin_g 维度
# 形状变为 (batch, 1, out_channels, cin_g, k1, k2)



完整修改位置参考

在 FDConv.forward 方法中，找到 adaptive_weights 的定义行，按上述代码替换即可。

附加建议

若后续在其他模块中遇到 torch.meshgrid 的 FutureWarning，可在调用时添加 indexing='ij' 参数以消除警告。

测试时建议逐步解锁 SimpleStem、XSSBlock、VSSBlock，验证完整模型的前向传播。

按此修改后，RGBlock 应能正常输出形状 (2, 128, 32, 32)，不再报错。

















我的mbyolo_train.py文件内容为：
from ultralytics import YOLO
import argparse
import os

ROOT = os.path.abspath('.') + "/"


def parse_opt():
   parser = argparse.ArgumentParser()
   parser.add_argument('--data', type=str, default=ROOT + '/ultralytics/cfg/datasets/coco.yaml', help='dataset.yaml path')
   parser.add_argument('--config', type=str, default=ROOT + '/ultralytics/cfg/models/mamba-yolo/Mamba-YOLO-T.yaml', help='model path(s)')
   parser.add_argument('--batch_size', type=int, default=512, help='batch size')
   parser.add_argument('--imgsz', '--img', '--img-size', type=int, default=640, help='inference size (pixels)')
   parser.add_argument('--task', default='train', help='train, val, test, speed or study')
   parser.add_argument('--device', default='0,1,2,3,4,5,6,7', help='cuda device, i.e. 0 or 0,1,2,3 or cpu')
   parser.add_argument('--workers', type=int, default=128, help='max dataloader workers (per RANK in DDP mode)')
   parser.add_argument('--epochs', type=int, default=300)
   parser.add_argument('--optimizer', default='SGD', help='SGD, Adam, AdamW')
   parser.add_argument('--amp', action='store_true', help='open amp')
   parser.add_argument('--project', default=ROOT + '/output_dir/mscoco', help='save to project/name')
   parser.add_argument('--name', default='mambayolo', help='save to project/name')
   parser.add_argument('--half', action='store_true', help='use FP16 half-precision inference')
   parser.add_argument('--dnn', action='store_true', help='use OpenCV DNN for ONNX inference')
   opt = parser.parse_args()
   return opt


if __name__ == '__main__':
   opt = parse_opt()
   task = opt.task
   args = {
       "data": ROOT + opt.data,
       "epochs": opt.epochs,
       "workers": opt.workers,
       "batch": opt.batch_size,
       "optimizer": opt.optimizer,
       "device": opt.device,
       "amp": opt.amp,
       "project": ROOT + opt.project,
       "name": opt.name,
   }
   model_conf = ROOT + opt.config
   task_type = {
       "train": YOLO(model_conf).train(**args),
       "val": YOLO(model_conf).val(**args),
       # "test": YOLO(model_conf).test(**args),
   }
   task_type.get(task)


运行报错：
(mambayolo) huangyh@user-ThinkStation-P920:~/Mamba-YOLO$ python mbyolo_train.py --task train --data ultralytics/cfg/datasets/coco.yaml  --config ultralytics/cfg/models/mamba-yolo/Mamba-YOLO-T.yaml --amp  --project ./output_dir/mscoco --name mambayolo_FDConv_RG_n --device 0 --batch_size 16 --epochs 1  
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/timm/models/layers/__init__.py:49: FutureWarning: Importing from timm.models.layers is deprecated, please import via timm.layers
 warnings.warn(f"Importing from {__name__} is deprecated, please import via timm.layers", FutureWarning)
WARNING ⚠️ no model scale passed. Assuming scale='T'.
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 85
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/functional.py:512: UserWarning: torch.meshgrid: in an upcoming release, it will be required to pass the indexing argument. (Triggered internally at ../aten/src/ATen/native/TensorShape.cpp:3587.)
 return _VF.meshgrid(tensors, **kwargs)  # type: ignore[attr-defined]
Traceback (most recent call last):
 File "/home/huangyh/Mamba-YOLO/mbyolo_train.py", line 44, in <module>
   "train": YOLO(model_conf).train(**args),
            ^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/models/yolo/model.py", line 23, in __init__
   super().__init__(model=model, task=task, verbose=verbose)
 File "/home/huangyh/Mamba-YOLO/ultralytics/engine/model.py", line 150, in __init__
   self._new(model, task=task, verbose=verbose)
 File "/home/huangyh/Mamba-YOLO/ultralytics/engine/model.py", line 219, in _new
   self.model = (model or self._smart_load("model"))(cfg_dict, verbose=verbose and RANK == -1)  # build model
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/tasks.py", line 290, in __init__
   self.model, self.save = parse_model(deepcopy(self.yaml), ch=ch, verbose=verbose)  # model, savelist
                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/tasks.py", line 945, in parse_model
   m_ = nn.Sequential(*(m(*args) for _ in range(n))) if n > 1 else m(*args)  # module
                                                                   ^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/mamba_yolo.py", line 468, in __init__
   self.mlp = RGBlock(in_features=hidden_dim, hidden_features=mlp_hidden_dim, act_layer=mlp_act_layer,
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/mamba_yolo.py", line 265, in __init__
   self.dwconv = FDConv(
                 ^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/FDConv.py", line 555, in __init__
   self.convert2dftweight(convert_param)
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/FDConv.py", line 591, in convert2dftweight
   indices.append(freq_indices.reshape(2, self.kernel_num, -1))
                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
RuntimeError: shape '[2, 4, -1]' is invalid for input of size 1020













错误发生在 FDConv.convert2dftweight 方法中，当 param_reduction == 1.0（默认值）时，频点索引 freq_indices 的总长度未能被 kernel_num 整除，导致 reshape 失败。

原因分析

freq_indices 由 get_fft2freq 生成，其形状为 (2, total_freqs)，total_freqs 等于 out_channels * k1 * (in_per_group * k2 // 2 + 1)。

在深度卷积场景下，total_freqs = 510，但 kernel_num = 4，510 不能被 4 整除。

在 param_reduction < 1 分支中已有逻辑将频点数调整为 kernel_num 的整数倍，但在 param_reduction == 1.0 的 else 分支中缺少此处理。

后续 freq_indices.reshape(2, self.kernel_num, -1) 要求总元素数能被 2 * kernel_num 整除，否则引发 RuntimeError。

解决方案

在 FDConv.convert2dftweight 的 else 分支中添加频点数对齐逻辑，使 freq_indices 长度变为 kernel_num 的整数倍，同时对应截断 weight_rfft。

修改位置： FDConv.py 中 convert2dftweight 函数，约第 585 行附近。

修改前：

python

        else:
            weight_rfft = torch.stack([weight_rfft.real, weight_rfft.imag], dim=-1)[None, ].repeat(
                self.param_ratio, 1, 1, 1,
            ) / self._denom



修改后：

python

        else:
            weight_rfft = torch.stack([weight_rfft.real, weight_rfft.imag], dim=-1)[None, ].repeat(
                self.param_ratio, 1, 1, 1,
            ) / self._denom
            # 确保频点数能被 kernel_num 整除
            total_freqs = freq_indices.size(1)
            num_to_keep = (total_freqs // self.kernel_num) * self.kernel_num
            if num_to_keep < total_freqs:
                freq_indices = freq_indices[:, :num_to_keep]
                weight_rfft = weight_rfft[:, :num_to_keep, :]



补充说明

此修改仅截断末尾少量频点（最多 kernel_num - 1 个），对模型精度影响可忽略。

修改后重新运行训练脚本，模型初始化应能通过，不再出现 reshape 错误。















根据你的建议我修改代码后跑训练仍报错：

(mambayolo) huangyh@user-ThinkStation-P920:~/Mamba-YOLO$ python mbyolo_train.py --task train --data ultralytics/cfg/datasets/coco.yaml  --config ultralytics/cfg/models/mamba-yolo/Mamba-YOLO-T.yaml --amp  --project ./output_dir/mscoco --name mambayolo_FDConv_RG_n --device 0 --batch_size 16 --epochs 1
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/timm/models/layers/__init__.py:49: FutureWarning: Importing from timm.models.layers is deprecated, please import via timm.layers
 warnings.warn(f"Importing from {__name__} is deprecated, please import via timm.layers", FutureWarning)
WARNING ⚠️ no model scale passed. Assuming scale='T'.
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 85
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/functional.py:512: UserWarning: torch.meshgrid: in an upcoming release, it will be required to pass the indexing argument. (Triggered internally at ../aten/src/ATen/native/TensorShape.cpp:3587.)
 return _VF.meshgrid(tensors, **kwargs)  # type: ignore[attr-defined]
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
Ultralytics YOLOv8.2.29 🚀 Python-3.11.15 torch-2.3.0+cu121 CUDA:0 (NVIDIA GeForce RTX 3090, 24257MiB)
engine/trainer: task=detect, mode=train, model=/home/huangyh/Mamba-YOLO/ultralytics/cfg/models/mamba-yolo/Mamba-YOLO-T.yaml, data=/home/huangyh/Mamba-YOLO/ultralytics/cfg/datasets/coco.yaml, epochs=1, time=None, patience=100, batch=16, imgsz=640, save=True, save_period=-1, cache=False, device=0, workers=128, project=/home/huangyh/Mamba-YOLO/./output_dir/mscoco, name=mambayolo_FDConv_RG_n, exist_ok=False, pretrained=True, optimizer=SGD, verbose=True, seed=0, deterministic=True, single_cls=False, rect=False, cos_lr=False, close_mosaic=10, resume=False, amp=True, fraction=1.0, profile=False, freeze=None, multi_scale=False, overlap_mask=True, mask_ratio=4, dropout=0.0, val=True, split=val, save_json=False, save_hybrid=False, conf=None, iou=0.7, max_det=300, half=False, dnn=False, plots=True, source=None, vid_stride=1, stream_buffer=False, visualize=False, augment=False, agnostic_nms=False, classes=None, retina_masks=False, embed=None, show=False, save_frames=False, save_txt=False, save_conf=False, save_crop=False, show_labels=True, show_conf=True, show_boxes=True, line_width=None, format=torchscript, keras=False, optimize=False, int8=False, dynamic=False, simplify=False, opset=None, workspace=4, nms=False, lr0=0.01, lrf=0.01, momentum=0.937, weight_decay=0.0005, warmup_epochs=3.0, warmup_momentum=0.8, warmup_bias_lr=0.1, box=7.5, cls=0.5, dfl=1.5, pose=12.0, kobj=1.0, label_smoothing=0.0, nbs=64, hsv_h=0.015, hsv_s=0.7, hsv_v=0.4, degrees=0.0, translate=0.1, scale=0.5, shear=0.0, perspective=0.0, flipud=0.0, fliplr=0.5, bgr=0.0, mosaic=1.0, mixup=0.0, copy_paste=0.0, auto_augment=randaugment, erasing=0.4, crop_fraction=1.0, cfg=None, tracker=botsort.yaml, save_dir=/home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n
WARNING ⚠️ no model scale passed. Assuming scale='T'.

                  from  n    params  module                                       arguments                     
 0                  -1  1      5136  ultralytics.nn.modules.mamba_yolo.SimpleStem [3, 32, 3]                    
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 85
 1                  -1  1     39322  ultralytics.nn.modules.mamba_yolo.VSSBlock   [32, 32]                      
 2                  -1  1      8384  ultralytics.nn.modules.mamba_yolo.VisionClueMerge[32, 64]                      
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
 3                  -1  1    115169  ultralytics.nn.modules.mamba_yolo.VSSBlock   [64, 64]                      
 4                  -1  1     33152  ultralytics.nn.modules.mamba_yolo.VisionClueMerge[64, 128]                     
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
 5                  -1  3   1133166  ultralytics.nn.modules.mamba_yolo.VSSBlock   [128, 128]                    
 6                  -1  1    131840  ultralytics.nn.modules.mamba_yolo.VisionClueMerge[128, 256]                    
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
 7                  -1  1   1344737  ultralytics.nn.modules.mamba_yolo.VSSBlock   [256, 256]                    
 8                  -1  1    164608  ultralytics.nn.modules.block.SPPF            [256, 256, 5]                 
 9                  -1  1         0  torch.nn.modules.upsampling.Upsample         [None, 2, 'nearest']          
10             [-1, 5]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
11                  -1  1    410362  ultralytics.nn.modules.mamba_yolo.XSSBlock   [384, 128, 1]                 
12                  -1  1         0  torch.nn.modules.upsampling.Upsample         [None, 2, 'nearest']          
13             [-1, 3]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
14                  -1  1    123297  ultralytics.nn.modules.mamba_yolo.XSSBlock   [192, 64, 1]                  
15                  -1  1     36992  ultralytics.nn.modules.conv.Conv             [64, 64, 3, 2]                
16            [-1, 11]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
17                  -1  1    385786  ultralytics.nn.modules.mamba_yolo.XSSBlock   [192, 128, 1]                 
18                  -1  1    147712  ultralytics.nn.modules.conv.Conv             [128, 128, 3, 2]              
19             [-1, 8]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
20                  -1  1   1377249  ultralytics.nn.modules.mamba_yolo.XSSBlock   [384, 256, 1]                 
21        [14, 17, 20]  1    897664  ultralytics.nn.modules.head.Detect           [80, [64, 128, 256]]          
Mamba-YOLO-T summary: 554 layers, 6354576 parameters, 6354560 gradients, 14.6 GFLOPs

Freezing layer 'model.21.dfl.conv.weight'
AMP: running Automatic Mixed Precision (AMP) checks with YOLOv8n...
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/conv.py:456: UserWarning: Plan failed with a cudnnException: CUDNN_BACKEND_EXECUTION_PLAN_DESCRIPTOR: cudnnFinalize Descriptor Failed cudnn_status: CUDNN_STATUS_NOT_SUPPORTED (Triggered internally at ../aten/src/ATen/native/cudnn/Conv_v8.cpp:919.)
 return F.conv2d(input, weight, bias, self.stride,
AMP: checks passed ✅
train: Scanning /home/huangyh/cocoapi/coco_balanced_subset_yolo/labels/train2017.cache... 10350 images, 0 backgrounds, 0 corrupt: 100%|██████████| 10350/10350 [00:00<?, ?it/s]
val: Scanning /home/huangyh/cocoapi/coco_balanced_subset_yolo/labels/val2017.cache... 3388 images, 0 backgrounds, 0 corrupt: 100%|██████████| 3388/3388 [00:00<?, ?it/s]
Plotting labels to /home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n/labels.jpg... 
optimizer: SGD(lr=0.01, momentum=0.937) with parameter groups 81 weight(decay=0.0), 258 weight(decay=0.0005), 256 bias(decay=0.0)
Image sizes 640 train, 640 val
Using 40 dataloader workers
Logging results to /home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n
Starting training for 1 epochs...

     Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
 0%|          | 0/647 [00:00<?, ?it/s]/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/conv.py:456: UserWarning: Plan failed with a cudnnException: CUDNN_BACKEND_EXECUTION_PLAN_DESCRIPTOR: cudnnFinalize Descriptor Failed cudnn_status: CUDNN_STATUS_NOT_SUPPORTED (Triggered internally at ../aten/src/ATen/native/cudnn/Conv_v8.cpp:919.)
 return F.conv2d(input, weight, bias, self.stride,
       1/1      14.5G      3.645       5.04      3.361        300        640: 100%|██████████| 647/647 [06:19<00:00,  1.71it/s]
                Class     Images  Instances      Box(P          R      mAP50  mAP50-95):   0%|          | 0/106 [00:00<?, ?it/s]
Traceback (most recent call last):
 File "/home/huangyh/Mamba-YOLO/mbyolo_train.py", line 44, in <module>
   "train": YOLO(model_conf).train(**args),
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/engine/model.py", line 674, in train
   self.trainer.train()
 File "/home/huangyh/Mamba-YOLO/ultralytics/engine/trainer.py", line 199, in train
   self._do_train(world_size)
 File "/home/huangyh/Mamba-YOLO/ultralytics/engine/trainer.py", line 424, in _do_train
   self.metrics, self.fitness = self.validate()
                                ^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/engine/trainer.py", line 565, in validate
   metrics = self.validator(self)
             ^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/utils/_contextlib.py", line 115, in decorate_context
   return func(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/engine/validator.py", line 178, in __call__
   preds = model(batch["img"], augment=augment)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/tasks.py", line 92, in forward
   return self.predict(x, *args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/tasks.py", line 110, in predict
   return self._predict_once(x, profile, visualize, embed)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/tasks.py", line 131, in _predict_once
   x = m(x)  # run
       ^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/mamba_yolo.py", line 476, in forward
   x = x + self.drop_path(self.mlp(self.norm2(x)))  # FFN
                          ^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/mamba_yolo.py", line 286, in forward
   x = self.fc2(x)
       ^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/conv.py", line 460, in forward
   return self._conv_forward(input, self.weight, self.bias)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/conv.py", line 456, in _conv_forward
   return F.conv2d(input, weight, bias, self.stride,
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
RuntimeError: Input type (float) and bias type (c10::Half) should be the same
















错误发生在验证阶段的 FDConv 输出添加 bias 时，由于 AMP 混合精度导致 output 为 FP32 而 self.bias 为 FP16，触发类型不匹配。

根本原因

在 FDConv.forward 中，DFT_map 被强制指定为 torch.float32，而 F.conv2d 返回类型遵循输入 x（训练时可能为 FP16）和 weight 的类型（DFT 计算强制 FP32），最终输出为 FP32。但 self.bias 在 AMP 下被自动转换为 FP16，相加时类型冲突。

解决方案

修改 FDConv.py 中的 forward 方法，确保数据类型一致性：

统一中间张量 dtype：将 DFT_map 的 dtype 设为与输入 x 一致。

添加 bias 时进行类型转换：将 self.bias 转换为 output.dtype 后再相加。

具体修改位置（约 635-690 行）

修改前：

python

DFT_map = torch.zeros(
    (b, self.out_channels * k1, cin_g * k2 // 2 + 1, 2),
    device=x.device, dtype=torch.float32,
)
...
if self.bias is not None:
    output = output + self.bias.view(1, -1, 1, 1)



修改后：

python

input_dtype = x.dtype
DFT_map = torch.zeros(
    (b, self.out_channels * k1, cin_g * k2 // 2 + 1, 2),
    device=x.device, dtype=input_dtype,
)
...
if self.bias is not None:
    output = output + self.bias.to(input_dtype).view(1, -1, 1, 1)



同时，确保后续 dft_weight 和 kernel_attention 运算前也转换至 input_dtype。

完整代码片段（参考）

python

def forward(self, x):
    if min(self.in_channels, self.out_channels) <= self.use_fdconv_if_c_gt \
            or self.kernel_size[0] not in self.use_fdconv_if_k_in:
        return super().forward(x)

    input_dtype = x.dtype
    # ... 中间计算 ...
    DFT_map = torch.zeros(
        (b, self.out_channels * k1, cin_g * k2 // 2 + 1, 2),
        device=x.device, dtype=input_dtype,
    )
    # ... 频域权重累加 ...
    adaptive_weights = torch.fft.irfft2(
        torch.view_as_complex(DFT_map),
        dim=(1, 2),
        s=(self.out_channels * k1, cin_g * k2)
    ).reshape(batch_size, 1, self.out_channels, k1, cin_g, k2)
    adaptive_weights = adaptive_weights.permute(0, 1, 2, 4, 3, 5)
    # ... 注意力聚合 ...
    output = F.conv2d(...)
    output = output.view(batch_size, self.out_channels, output.size(-2), output.size(-1))
    if self.bias is not None:
        output = output + self.bias.to(input_dtype).view(1, -1, 1, 1)
    return output



附加说明

若仍出现 cudnnException 警告，可暂时忽略或设置环境变量 TORCH_CUDNN_V8_API_DISABLED=1。

修改后重新运行训练，验证阶段应能正常通过。













(mambayolo) huangyh@user-ThinkStation-P920:~/Mamba-YOLO$ python mbyolo_train.py --task train --data ultralytics/cfg/datasets/coco.yaml  --config ultralytics/cfg/models/mamba-yolo/Mamba-YOLO-T.yaml --amp  --project ./output_dir/mscoco --name mambayolo_FDConv_RG_n --device 0 --batch_size 16 --epochs 1
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/timm/models/layers/__init__.py:49: FutureWarning: Importing from timm.models.layers is deprecated, please import via timm.layers
 warnings.warn(f"Importing from {__name__} is deprecated, please import via timm.layers", FutureWarning)
WARNING ⚠️ no model scale passed. Assuming scale='T'.
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 85
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/functional.py:512: UserWarning: torch.meshgrid: in an upcoming release, it will be required to pass the indexing argument. (Triggered internally at ../aten/src/ATen/native/TensorShape.cpp:3587.)
 return _VF.meshgrid(tensors, **kwargs)  # type: ignore[attr-defined]
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
Ultralytics YOLOv8.2.29 🚀 Python-3.11.15 torch-2.3.0+cu121 CUDA:0 (NVIDIA GeForce RTX 3090, 24257MiB)
engine/trainer: task=detect, mode=train, model=/home/huangyh/Mamba-YOLO/ultralytics/cfg/models/mamba-yolo/Mamba-YOLO-T.yaml, data=/home/huangyh/Mamba-YOLO/ultralytics/cfg/datasets/coco.yaml, epochs=1, time=None, patience=100, batch=16, imgsz=640, save=True, save_period=-1, cache=False, device=0, workers=128, project=/home/huangyh/Mamba-YOLO/./output_dir/mscoco, name=mambayolo_FDConv_RG_n2, exist_ok=False, pretrained=True, optimizer=SGD, verbose=True, seed=0, deterministic=True, single_cls=False, rect=False, cos_lr=False, close_mosaic=10, resume=False, amp=True, fraction=1.0, profile=False, freeze=None, multi_scale=False, overlap_mask=True, mask_ratio=4, dropout=0.0, val=True, split=val, save_json=False, save_hybrid=False, conf=None, iou=0.7, max_det=300, half=False, dnn=False, plots=True, source=None, vid_stride=1, stream_buffer=False, visualize=False, augment=False, agnostic_nms=False, classes=None, retina_masks=False, embed=None, show=False, save_frames=False, save_txt=False, save_conf=False, save_crop=False, show_labels=True, show_conf=True, show_boxes=True, line_width=None, format=torchscript, keras=False, optimize=False, int8=False, dynamic=False, simplify=False, opset=None, workspace=4, nms=False, lr0=0.01, lrf=0.01, momentum=0.937, weight_decay=0.0005, warmup_epochs=3.0, warmup_momentum=0.8, warmup_bias_lr=0.1, box=7.5, cls=0.5, dfl=1.5, pose=12.0, kobj=1.0, label_smoothing=0.0, nbs=64, hsv_h=0.015, hsv_s=0.7, hsv_v=0.4, degrees=0.0, translate=0.1, scale=0.5, shear=0.0, perspective=0.0, flipud=0.0, fliplr=0.5, bgr=0.0, mosaic=1.0, mixup=0.0, copy_paste=0.0, auto_augment=randaugment, erasing=0.4, crop_fraction=1.0, cfg=None, tracker=botsort.yaml, save_dir=/home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n2
WARNING ⚠️ no model scale passed. Assuming scale='T'.

                  from  n    params  module                                       arguments                     
 0                  -1  1      5136  ultralytics.nn.modules.mamba_yolo.SimpleStem [3, 32, 3]                    
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 85
 1                  -1  1     39322  ultralytics.nn.modules.mamba_yolo.VSSBlock   [32, 32]                      
 2                  -1  1      8384  ultralytics.nn.modules.mamba_yolo.VisionClueMerge[32, 64]                      
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
 3                  -1  1    115169  ultralytics.nn.modules.mamba_yolo.VSSBlock   [64, 64]                      
 4                  -1  1     33152  ultralytics.nn.modules.mamba_yolo.VisionClueMerge[64, 128]                     
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
 5                  -1  3   1133166  ultralytics.nn.modules.mamba_yolo.VSSBlock   [128, 128]                    
 6                  -1  1    131840  ultralytics.nn.modules.mamba_yolo.VisionClueMerge[128, 256]                    
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
 7                  -1  1   1344737  ultralytics.nn.modules.mamba_yolo.VSSBlock   [256, 256]                    
 8                  -1  1    164608  ultralytics.nn.modules.block.SPPF            [256, 256, 5]                 
 9                  -1  1         0  torch.nn.modules.upsampling.Upsample         [None, 2, 'nearest']          
10             [-1, 5]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
11                  -1  1    410362  ultralytics.nn.modules.mamba_yolo.XSSBlock   [384, 128, 1]                 
12                  -1  1         0  torch.nn.modules.upsampling.Upsample         [None, 2, 'nearest']          
13             [-1, 3]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
14                  -1  1    123297  ultralytics.nn.modules.mamba_yolo.XSSBlock   [192, 64, 1]                  
15                  -1  1     36992  ultralytics.nn.modules.conv.Conv             [64, 64, 3, 2]                
16            [-1, 11]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
17                  -1  1    385786  ultralytics.nn.modules.mamba_yolo.XSSBlock   [192, 128, 1]                 
18                  -1  1    147712  ultralytics.nn.modules.conv.Conv             [128, 128, 3, 2]              
19             [-1, 8]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
20                  -1  1   1377249  ultralytics.nn.modules.mamba_yolo.XSSBlock   [384, 256, 1]                 
21        [14, 17, 20]  1    897664  ultralytics.nn.modules.head.Detect           [80, [64, 128, 256]]          
Mamba-YOLO-T summary: 554 layers, 6354576 parameters, 6354560 gradients, 14.6 GFLOPs

Freezing layer 'model.21.dfl.conv.weight'
AMP: running Automatic Mixed Precision (AMP) checks with YOLOv8n...
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/conv.py:456: UserWarning: Plan failed with a cudnnException: CUDNN_BACKEND_EXECUTION_PLAN_DESCRIPTOR: cudnnFinalize Descriptor Failed cudnn_status: CUDNN_STATUS_NOT_SUPPORTED (Triggered internally at ../aten/src/ATen/native/cudnn/Conv_v8.cpp:919.)
 return F.conv2d(input, weight, bias, self.stride,
AMP: checks passed ✅
train: Scanning /home/huangyh/cocoapi/coco_balanced_subset_yolo/labels/train2017.cache... 10350 images, 0 backgrounds, 0 corrupt: 100%|██████████| 10350/10350 [00:00<?, ?it/s]
val: Scanning /home/huangyh/cocoapi/coco_balanced_subset_yolo/labels/val2017.cache... 3388 images, 0 backgrounds, 0 corrupt: 100%|██████████| 3388/3388 [00:00<?, ?it/s]
Plotting labels to /home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n2/labels.jpg... 
optimizer: SGD(lr=0.01, momentum=0.937) with parameter groups 81 weight(decay=0.0), 258 weight(decay=0.0005), 256 bias(decay=0.0)
Image sizes 640 train, 640 val
Using 40 dataloader workers
Logging results to /home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n2
Starting training for 1 epochs...

     Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
 0%|          | 0/647 [00:00<?, ?it/s]/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/conv.py:456: UserWarning: Plan failed with a cudnnException: CUDNN_BACKEND_EXECUTION_PLAN_DESCRIPTOR: cudnnFinalize Descriptor Failed cudnn_status: CUDNN_STATUS_NOT_SUPPORTED (Triggered internally at ../aten/src/ATen/native/cudnn/Conv_v8.cpp:919.)
 return F.conv2d(input, weight, bias, self.stride,
/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/FDConv.py:680: UserWarning: ComplexHalf support is experimental and many operators don't support it yet. (Triggered internally at ../aten/src/ATen/EmptyTensor.cpp:41.)
 adaptive_weights = torch.fft.irfft2(
 0%|          | 0/647 [00:00<?, ?it/s]
Traceback (most recent call last):
 File "/home/huangyh/Mamba-YOLO/mbyolo_train.py", line 44, in <module>
   "train": YOLO(model_conf).train(**args),
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/engine/model.py", line 674, in train
   self.trainer.train()
 File "/home/huangyh/Mamba-YOLO/ultralytics/engine/trainer.py", line 199, in train
   self._do_train(world_size)
 File "/home/huangyh/Mamba-YOLO/ultralytics/engine/trainer.py", line 376, in _do_train
   self.loss, self.loss_items = self.model(batch)
                                ^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/tasks.py", line 91, in forward
   return self.loss(x, *args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/tasks.py", line 269, in loss
   preds = self.forward(batch["img"]) if preds is None else preds
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/tasks.py", line 92, in forward
   return self.predict(x, *args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/tasks.py", line 110, in predict
   return self._predict_once(x, profile, visualize, embed)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/tasks.py", line 131, in _predict_once
   x = m(x)  # run
       ^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/mamba_yolo.py", line 476, in forward
   x = x + self.drop_path(self.mlp(self.norm2(x)))  # FFN
                          ^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/mamba_yolo.py", line 284, in forward
   x = self.act(self.dwconv(x) + x) * v
                ^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1532, in _wrapped_call_impl
   return self._call_impl(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/module.py", line 1541, in _call_impl
   return forward_call(*args, **kwargs)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 File "/home/huangyh/Mamba-YOLO/ultralytics/nn/modules/FDConv.py", line 680, in forward
   adaptive_weights = torch.fft.irfft2(
                      ^^^^^^^^^^^^^^^^^
RuntimeError: "fill_empty_deterministic_" not implemented for 'ComplexHalf'













我们注意到错误发生在新位置：torch.fft.irfft2 不支持复数半精度（ComplexHalf）操作。原因是之前修改中将 DFT_map 的 dtype 设为与输入 x 一致（在 AMP 下为 FP16），导致出现 ComplexHalf 张量，而 PyTorch 的 FFT 操作对 ComplexHalf 支持有限或未实现。

需要确保 DFT 相关计算使用 FP32，但在最后卷积输出时与输入/偏置类型匹配。解决方案：保持 DFT 计算在 FP32 下进行，仅将最终输出转换为输入 dtype，偏置也作相应转换。

具体修改建议：

DFT_map 始终使用 torch.float32（或 torch.complex64）。

计算 adaptive_weights 时也在 FP32 下进行。

聚合后的 aggregate_weight 在传入 F.conv2d 前转换为 input_dtype，确保卷积输出与输入类型一致。

偏置 self.bias 转换为 output.dtype。

修改后的 FDConv.forward 关键部分参考：

python

def forward(self, x):
    if min(self.in_channels, self.out_channels) <= self.use_fdconv_if_c_gt \
            or self.kernel_size[0] not in self.use_fdconv_if_k_in:
        return super().forward(x)

    input_dtype = x.dtype
    # ... 全局注意力计算（注意 att_feat 类型） ...
    
    # DFT 部分强制使用 float32
    DFT_map = torch.zeros(
        (b, self.out_channels * k1, cin_g * k2 // 2 + 1, 2),
        device=x.device, dtype=torch.float32,
    )
    # ... 频域累加（dft_weight 与 kernel_attention 可能需转 float32） ...
    
    # FFT 逆变换得到 float32 权重
    adaptive_weights = torch.fft.irfft2(
        torch.view_as_complex(DFT_map),
        dim=(1, 2),
        s=(self.out_channels * k1, cin_g * k2)
    ).reshape(batch_size, 1, self.out_channels, k1, cin_g, k2)
    adaptive_weights = adaptive_weights.permute(0, 1, 2, 4, 3, 5)  # (b,1,cout,cin_g,k,k)
    
    # 聚合权重 (保持 float32 计算)
    aggregate_weight = spatial_attention * channel_attention * filter_attention * adaptive_weights * hr_att
    aggregate_weight = torch.sum(aggregate_weight, dim=1)
    aggregate_weight = aggregate_weight.reshape(batch_size * self.out_channels, cin_g, k1, k2)
    
    # 转换为输入 dtype 后再卷积
    aggregate_weight = aggregate_weight.to(input_dtype)
    x_conv = x.reshape(1, -1, height, width).to(input_dtype)
    output = F.conv2d(
        x_conv, weight=aggregate_weight, bias=None,
        stride=self.stride, padding=self.padding,
        dilation=self.dilation, groups=self.groups * batch_size,
    )
    output = output.view(batch_size, self.out_channels, output.size(-2), output.size(-1))
    if self.bias is not None:
        output = output + self.bias.to(output.dtype).view(1, -1, 1, 1)
    return output



额外注意：

hr_att 等注意力张量可能是在 global_x 基础上计算的，需确保 global_x 也转为 float32 参与 DFT 部分，或直接利用 to(torch.float32)。

FBM 输出应保持与输入一致类型。

按照上述思路调整后，既能避免 ComplexHalf 问题，又能通过类型转换保证 bias 相加兼容。修改后重新运行训练脚本。









(mambayolo) huangyh@user-ThinkStation-P920:~/Mamba-YOLO$ python mbyolo_train.py --task train --data ultralytics/cfg/datasets/coco.yaml  --config ultralytics/cfg/models/mamba-yolo/Mamba-YOLO-T.yaml --amp  --project ./output_dir/mscoco --name mambayolo_FDConv_RG_n --device 0 --batch_size 16 --epochs 1
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/timm/models/layers/__init__.py:49: FutureWarning: Importing from timm.models.layers is deprecated, please import via timm.layers
 warnings.warn(f"Importing from {__name__} is deprecated, please import via timm.layers", FutureWarning)
WARNING ⚠️ no model scale passed. Assuming scale='T'.
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 85
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/functional.py:512: UserWarning: torch.meshgrid: in an upcoming release, it will be required to pass the indexing argument. (Triggered internally at ../aten/src/ATen/native/TensorShape.cpp:3587.)
 return _VF.meshgrid(tensors, **kwargs)  # type: ignore[attr-defined]
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
Ultralytics YOLOv8.2.29 🚀 Python-3.11.15 torch-2.3.0+cu121 CUDA:0 (NVIDIA GeForce RTX 3090, 24257MiB)
engine/trainer: task=detect, mode=train, model=/home/huangyh/Mamba-YOLO/ultralytics/cfg/models/mamba-yolo/Mamba-YOLO-T.yaml, data=/home/huangyh/Mamba-YOLO/ultralytics/cfg/datasets/coco.yaml, epochs=1, time=None, patience=100, batch=16, imgsz=640, save=True, save_period=-1, cache=False, device=0, workers=128, project=/home/huangyh/Mamba-YOLO/./output_dir/mscoco, name=mambayolo_FDConv_RG_n3, exist_ok=False, pretrained=True, optimizer=SGD, verbose=True, seed=0, deterministic=True, single_cls=False, rect=False, cos_lr=False, close_mosaic=10, resume=False, amp=True, fraction=1.0, profile=False, freeze=None, multi_scale=False, overlap_mask=True, mask_ratio=4, dropout=0.0, val=True, split=val, save_json=False, save_hybrid=False, conf=None, iou=0.7, max_det=300, half=False, dnn=False, plots=True, source=None, vid_stride=1, stream_buffer=False, visualize=False, augment=False, agnostic_nms=False, classes=None, retina_masks=False, embed=None, show=False, save_frames=False, save_txt=False, save_conf=False, save_crop=False, show_labels=True, show_conf=True, show_boxes=True, line_width=None, format=torchscript, keras=False, optimize=False, int8=False, dynamic=False, simplify=False, opset=None, workspace=4, nms=False, lr0=0.01, lrf=0.01, momentum=0.937, weight_decay=0.0005, warmup_epochs=3.0, warmup_momentum=0.8, warmup_bias_lr=0.1, box=7.5, cls=0.5, dfl=1.5, pose=12.0, kobj=1.0, label_smoothing=0.0, nbs=64, hsv_h=0.015, hsv_s=0.7, hsv_v=0.4, degrees=0.0, translate=0.1, scale=0.5, shear=0.0, perspective=0.0, flipud=0.0, fliplr=0.5, bgr=0.0, mosaic=1.0, mixup=0.0, copy_paste=0.0, auto_augment=randaugment, erasing=0.4, crop_fraction=1.0, cfg=None, tracker=botsort.yaml, save_dir=/home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n3
WARNING ⚠️ no model scale passed. Assuming scale='T'.

                  from  n    params  module                                       arguments                     
 0                  -1  1      5136  ultralytics.nn.modules.mamba_yolo.SimpleStem [3, 32, 3]                    
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 85
 1                  -1  1     39322  ultralytics.nn.modules.mamba_yolo.VSSBlock   [32, 32]                      
 2                  -1  1      8384  ultralytics.nn.modules.mamba_yolo.VisionClueMerge[32, 64]                      
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
 3                  -1  1    115169  ultralytics.nn.modules.mamba_yolo.VSSBlock   [64, 64]                      
 4                  -1  1     33152  ultralytics.nn.modules.mamba_yolo.VisionClueMerge[64, 128]                     
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
 5                  -1  3   1133166  ultralytics.nn.modules.mamba_yolo.VSSBlock   [128, 128]                    
 6                  -1  1    131840  ultralytics.nn.modules.mamba_yolo.VisionClueMerge[128, 256]                    
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
 7                  -1  1   1344737  ultralytics.nn.modules.mamba_yolo.VSSBlock   [256, 256]                    
 8                  -1  1    164608  ultralytics.nn.modules.block.SPPF            [256, 256, 5]                 
 9                  -1  1         0  torch.nn.modules.upsampling.Upsample         [None, 2, 'nearest']          
10             [-1, 5]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
11                  -1  1    410362  ultralytics.nn.modules.mamba_yolo.XSSBlock   [384, 128, 1]                 
12                  -1  1         0  torch.nn.modules.upsampling.Upsample         [None, 2, 'nearest']          
13             [-1, 3]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
14                  -1  1    123297  ultralytics.nn.modules.mamba_yolo.XSSBlock   [192, 64, 1]                  
15                  -1  1     36992  ultralytics.nn.modules.conv.Conv             [64, 64, 3, 2]                
16            [-1, 11]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
17                  -1  1    385786  ultralytics.nn.modules.mamba_yolo.XSSBlock   [192, 128, 1]                 
18                  -1  1    147712  ultralytics.nn.modules.conv.Conv             [128, 128, 3, 2]              
19             [-1, 8]  1         0  ultralytics.nn.modules.conv.Concat           [1]                           
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
20                  -1  1   1377249  ultralytics.nn.modules.mamba_yolo.XSSBlock   [384, 256, 1]                 
21        [14, 17, 20]  1    897664  ultralytics.nn.modules.head.Detect           [80, [64, 128, 256]]          
Mamba-YOLO-T summary: 554 layers, 6354576 parameters, 6354560 gradients, 14.6 GFLOPs

Freezing layer 'model.21.dfl.conv.weight'
AMP: running Automatic Mixed Precision (AMP) checks with YOLOv8n...
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/conv.py:456: UserWarning: Plan failed with a cudnnException: CUDNN_BACKEND_EXECUTION_PLAN_DESCRIPTOR: cudnnFinalize Descriptor Failed cudnn_status: CUDNN_STATUS_NOT_SUPPORTED (Triggered internally at ../aten/src/ATen/native/cudnn/Conv_v8.cpp:919.)
 return F.conv2d(input, weight, bias, self.stride,
AMP: checks passed ✅
train: Scanning /home/huangyh/cocoapi/coco_balanced_subset_yolo/labels/train2017.cache... 10350 images, 0 backgrounds, 0 corrupt: 100%|██████████| 10350/10350 [00:00<?, ?it/s]
val: Scanning /home/huangyh/cocoapi/coco_balanced_subset_yolo/labels/val2017.cache... 3388 images, 0 backgrounds, 0 corrupt: 100%|██████████| 3388/3388 [00:00<?, ?it/s]
Plotting labels to /home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n3/labels.jpg... 
optimizer: SGD(lr=0.01, momentum=0.937) with parameter groups 81 weight(decay=0.0), 258 weight(decay=0.0005), 256 bias(decay=0.0)
Image sizes 640 train, 640 val
Using 40 dataloader workers
Logging results to /home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n3
Starting training for 1 epochs...

     Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
 0%|          | 0/647 [00:00<?, ?it/s]/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/conv.py:456: UserWarning: Plan failed with a cudnnException: CUDNN_BACKEND_EXECUTION_PLAN_DESCRIPTOR: cudnnFinalize Descriptor Failed cudnn_status: CUDNN_STATUS_NOT_SUPPORTED (Triggered internally at ../aten/src/ATen/native/cudnn/Conv_v8.cpp:919.)
 return F.conv2d(input, weight, bias, self.stride,
       1/1        14G      3.634      5.024      3.354        300        640: 100%|██████████| 647/647 [06:09<00:00,  1.75it/s]
                Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100%|██████████| 106/106 [00:46<00:00,  2.30it/s]
                  all       3388      27739    0.00106     0.0135   0.000823   0.000178

1 epochs completed in 0.117 hours.
Optimizer stripped from /home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n3/weights/last.pt, 13.5MB
Optimizer stripped from /home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n3/weights/best.pt, 13.5MB

Validating /home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n3/weights/best.pt...
Ultralytics YOLOv8.2.29 🚀 Python-3.11.15 torch-2.3.0+cu121 CUDA:0 (NVIDIA GeForce RTX 3090, 24257MiB)
Mamba-YOLO-T summary: 538 layers, 6353136 parameters, 0 gradients, 14.6 GFLOPs
/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/conv.py:456: UserWarning: Plan failed with a cudnnException: CUDNN_BACKEND_EXECUTION_PLAN_DESCRIPTOR: cudnnFinalize Descriptor Failed cudnn_status: CUDNN_STATUS_NOT_SUPPORTED (Triggered internally at ../aten/src/ATen/native/cudnn/Conv_v8.cpp:919.)
 return F.conv2d(input, weight, bias, self.stride,
                Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100%|██████████| 106/106 [00:37<00:00,  2.79it/s]
                  all       3388      27739    0.00106     0.0135   0.000823   0.000178
               person       1895       8135    0.00467      0.516     0.0197    0.00534
              bicycle        109        258          0          0          0          0
                  car        367       1309    0.00128    0.00917   0.000683   0.000197
           motorcycle         86        285          0          0          0          0
             airplane         97        143          0          0          0          0
                  bus        145        239          0          0          0          0
                train        157        190    0.00359     0.0158    0.00192   0.000433
                truck        161        296          0          0          0          0
                 boat         62        328          0          0          0          0
        traffic light        112        413          0          0          0          0
         fire hydrant         80         95          0          0          0          0
            stop sign         68         74    0.00515      0.108    0.00384   0.000926
        parking meter         37         60          0          0          0          0
                bench        142        290          0          0          0          0
                 bird         56        314          0          0          0          0
                  cat        146        164    0.00177     0.0427    0.00259   0.000837
                  dog        152        193    0.00394    0.00518    0.00198   0.000594
                horse        116        260     0.0143    0.00769    0.00768    0.00118
                sheep         43        318          0          0          0          0
                  cow         51        313          0          0          0          0
             elephant         79        242          0          0          0          0
                 bear         49         71   0.000949      0.211   0.000963   0.000282
                zebra         70        251          0          0          0          0
              giraffe        101        232    0.00213    0.00431    0.00107   0.000214
             backpack        165        279          0          0          0          0
             umbrella        101        331          0          0          0          0
              handbag        196        372          0          0          0          0
                  tie        134        241          0          0          0          0
             suitcase         63        243          0          0          0          0
              frisbee         84        115          0          0          0          0
                 skis         62        183          0          0          0          0
            snowboard         49         69          0          0          0          0
          sports ball        127        210          0          0          0          0
                 kite         66        302          0          0          0          0
         baseball bat         97        145          0          0          0          0
       baseball glove         70        117          0          0          0          0
           skateboard        125        177          0          0          0          0
            surfboard        147        265          0          0          0          0
        tennis racket        165        223          0          0          0          0
               bottle        266        661          0          0          0          0
           wine glass         72        253          0          0          0          0
                  cup        255        612          0          0          0          0
                 fork        130        190          0          0          0          0
                knife        128        255          0          0          0          0
                spoon        102        190          0          0          0          0
                 bowl        204        428          0          0          0          0
               banana         54        276          0          0          0          0
                apple         54        208    0.00175    0.00962   0.000887   0.000221
             sandwich         85        164          0          0          0          0
               orange         44        195          0          0          0          0
             broccoli         42        257          0          0          0          0
               carrot         50        247     0.0278    0.00405      0.014     0.0014
              hot dog         51        125          0          0          0          0
                pizza         75        196          0          0          0          0
                donut         29        250          0          0          0          0
                 cake         61        201          0          0          0          0
                chair        393       1250    0.00211     0.0296    0.00195   0.000703
                couch        134        200    0.00138       0.02   0.000759   0.000236
         potted plant        107        226          0          0          0          0
                  bed        144        158    0.00836      0.057    0.00445   0.000924
         dining table        308        445    0.00587     0.0382    0.00345   0.000793
               toilet        147        177          0          0          0          0
                   tv        150        218          0          0          0          0
               laptop        128        176          0          0          0          0
                mouse         86        104          0          0          0          0
               remote         86        195          0          0          0          0
             keyboard         82        126          0          0          0          0
           cell phone        130        178          0          0          0          0
            microwave         49         50          0          0          0          0
                 oven         78        106          0          0          0          0
              toaster          8          9          0          0          0          0
                 sink        147        177          0          0          0          0
         refrigerator         77        102          0          0          0          0
                 book        151        742          0          0          0          0
                clock        140        203          0          0          0          0
                 vase         68        173          0          0          0          0
             scissors         27         35          0          0          0          0
           teddy bear         74        170          0          0          0          0
           hair drier          9         11          0          0          0          0
           toothbrush         32         55          0          0          0          0
Speed: 0.1ms preprocess, 8.2ms inference, 0.0ms loss, 0.9ms postprocess per image
Results saved to /home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n3
WARNING ⚠️ no model scale passed. Assuming scale='T'.
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 85
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 170
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 341
*** FDConv kernel_num: 4 | in_channels_per_group: 1 | groups: 682
Ultralytics YOLOv8.2.29 🚀 Python-3.11.15 torch-2.3.0+cu121 CUDA:0 (NVIDIA GeForce RTX 3090, 24257MiB)
Mamba-YOLO-T summary: 538 layers, 6353136 parameters, 5139520 gradients, 14.6 GFLOPs
val: Scanning /home/huangyh/cocoapi/coco_balanced_subset_yolo/labels/val2017.cache... 3388 images, 0 backgrounds, 0 corrupt: 100%|██████████| 3388/3388 [00:00<?, ?it/s]
                Class     Images  Instances      Box(P          R      mAP50  mAP50-95):   0%|          | 0/212 [00:00<?, ?it/s]/home/huangyh/miniconda3/envs/mambayolo/lib/python3.11/site-packages/torch/nn/modules/conv.py:456: UserWarning: Plan failed with a cudnnException: CUDNN_BACKEND_EXECUTION_PLAN_DESCRIPTOR: cudnnFinalize Descriptor Failed cudnn_status: CUDNN_STATUS_NOT_SUPPORTED (Triggered internally at ../aten/src/ATen/native/cudnn/Conv_v8.cpp:919.)
 return F.conv2d(input, weight, bias, self.stride,
                Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100%|██████████| 212/212 [00:43<00:00,  4.90it/s]
                  all       3388      27739          0          0          0          0
Speed: 0.1ms preprocess, 11.7ms inference, 0.0ms loss, 0.1ms postprocess per image
Results saved to /home/huangyh/Mamba-YOLO/output_dir/mscoco/mambayolo_FDConv_RG_n4